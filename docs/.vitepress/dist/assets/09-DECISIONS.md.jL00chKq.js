import{_ as t,o as a,c as o,a2 as n}from"./chunks/framework.CZo9GF0W.js";const p=JSON.parse('{"title":"09 — Decision Log (ADRs)","description":"","frontmatter":{},"headers":[],"relativePath":"09-DECISIONS.md","filePath":"09-DECISIONS.md","lastUpdated":1789060268000}'),s={name:"09-DECISIONS.md"};function r(i,e,d,c,h,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="_09-—-decision-log-adrs" tabindex="-1">09 — Decision Log (ADRs) <a class="header-anchor" href="#_09-—-decision-log-adrs" aria-label="Permalink to &quot;09 — Decision Log (ADRs)&quot;">​</a></h1><h2 id="adr-001-·-pluggable-driver-registry-from-day-one-postgres-shipped-first" tabindex="-1">ADR-001 · Pluggable driver registry from day one, Postgres shipped first <a class="header-anchor" href="#adr-001-·-pluggable-driver-registry-from-day-one-postgres-shipped-first" aria-label="Permalink to &quot;ADR-001 · Pluggable driver registry from day one, Postgres shipped first&quot;">​</a></h2><p><strong>Decided.</strong> Define <code>DriverAdapter</code> + capability flags in Phase 1; only <code>driver-pg</code> exists until Phase 9. <strong>Why:</strong> retrofitting multi-engine support after a Postgres-shaped core is the failure mode that produced every &quot;PostgreSQL-only forever&quot; extension. Cost is ~1 week of interface design; the alternative is a rewrite. <strong>Guard:</strong> <code>dependency-cruiser</code> forbids <code>core</code>/<code>extension</code> importing <code>pg</code>, and forbids <code>if (driver.id === …)</code> branching (lint rule). Phase 9&#39;s SQLite PR must not touch core.</p><h2 id="adr-002-·-webview-react-19-tanstack-virtual-for-heavy-panels" tabindex="-1">ADR-002 · Webview + React 19 + TanStack Virtual for heavy panels <a class="header-anchor" href="#adr-002-·-webview-react-19-tanstack-virtual-for-heavy-panels" aria-label="Permalink to &quot;ADR-002 · Webview + React 19 + TanStack Virtual for heavy panels&quot;">​</a></h2><p><strong>Decided.</strong><strong>Why:</strong> native VS Code UI has no data grid, no inline cell editing, no canvas. TreeView/QuickPick cover the explorer; everything else needs a webview. React chosen over Lit for the ecosystem around virtualized tables and the ERD canvas. <strong>Cost:</strong> webview cold-start (~100–200 ms) and CSP discipline. Mitigated by keeping activation lazy and panels retained.</p><h2 id="adr-003-·-node-extension-host-only-no-web-extension-build-for-v1" tabindex="-1">ADR-003 · Node extension host only; no web extension build for v1 <a class="header-anchor" href="#adr-003-·-node-extension-host-only-no-web-extension-build-for-v1" aria-label="Permalink to &quot;ADR-003 · Node extension host only; no web extension build for v1&quot;">​</a></h2><p><strong>Decided.</strong><strong>Why:</strong> node-postgres needs TCP sockets. A browser build requires a WebSocket proxy the user must run — a different product. Revisit once demand is proven. <strong>Consequence:</strong> <code>&quot;extensionKind&quot;: [&quot;workspace&quot;]</code>, and the Remote-SSH &quot;localhost means the remote&quot; caveat gets top billing in the docs.</p><h2 id="adr-004-·-numeric-int8-never-touch-js-number" tabindex="-1">ADR-004 · <code>numeric</code>/<code>int8</code> never touch JS <code>Number</code> <a class="header-anchor" href="#adr-004-·-numeric-int8-never-touch-js-number" aria-label="Permalink to &quot;ADR-004 · `numeric`/`int8` never touch JS `Number`&quot;">​</a></h2><p><strong>Decided.</strong> Parsed to string / <code>bigint</code>, rendered and edited as string. <strong>Why:</strong> silent precision loss on money columns is the worst class of bug a DB client can have, and most existing extensions have it. <strong>Cost:</strong> sorting and filtering on those columns is server-side or string/BigInt-comparison based; slightly more work in the grid.</p><h2 id="adr-005-·-secrets-only-in-vscode-secretstorage" tabindex="-1">ADR-005 · Secrets only in <code>vscode.SecretStorage</code> <a class="header-anchor" href="#adr-005-·-secrets-only-in-vscode-secretstorage" aria-label="Permalink to &quot;ADR-005 · Secrets only in `vscode.SecretStorage`&quot;">​</a></h2><p><strong>Decided.</strong> Workspace connection files hold <code>SecretRef</code>s; the JSON Schema rejects literal passwords. <strong>Why:</strong> committed credentials are the #1 real-world harm from repo-shareable DB configs. Making the <em>file format itself</em> incapable of holding a secret beats documentation.</p><h2 id="adr-006-·-environment-tagging-write-guards-are-core-not-a-nice-to-have" tabindex="-1">ADR-006 · Environment tagging + write guards are core, not a nice-to-have <a class="header-anchor" href="#adr-006-·-environment-tagging-write-guards-are-core-not-a-nice-to-have" aria-label="Permalink to &quot;ADR-006 · Environment tagging + write guards are core, not a nice-to-have&quot;">​</a></h2><p><strong>Decided.</strong> <code>dev|staging|prod</code> on every profile, color everywhere, typed confirmation for destructive statements on prod, unqualified DML blocked by default. <strong>Why:</strong> the single most-cited fear about editor-embedded DB clients. Cheap to build, strong differentiator.</p><h2 id="adr-007-·-real-postgres-parser-libpg-query-wasm-not-regex" tabindex="-1">ADR-007 · Real Postgres parser (<code>libpg-query</code> WASM), not regex <a class="header-anchor" href="#adr-007-·-real-postgres-parser-libpg-query-wasm-not-regex" aria-label="Permalink to &quot;ADR-007 · Real Postgres parser (`libpg-query` WASM), not regex&quot;">​</a></h2><p><strong>Decided.</strong> Phase 4 introduces it; Phase 2 ships a documented heuristic splitter as a stopgap. <strong>Why:</strong> dollar-quoting, nested comments, and <code>E&#39;&#39;</code> defeat regex splitting, and guard classification must not be fooled by a comment containing <code>DROP TABLE</code>. <strong>Cost:</strong> ~1.5 MB WASM, lazily loaded.</p><h2 id="adr-008-·-server-side-cursors-columnar-store-instead-of-buffering-rows" tabindex="-1">ADR-008 · Server-side cursors + columnar store instead of buffering rows <a class="header-anchor" href="#adr-008-·-server-side-cursors-columnar-store-instead-of-buffering-rows" aria-label="Permalink to &quot;ADR-008 · Server-side cursors + columnar store instead of buffering rows&quot;">​</a></h2><p><strong>Decided.</strong><strong>Why:</strong> the shared failure of existing extensions is materializing every row as a JS object. Cursor + columnar buffers is the difference between 50k rows and 1M. <strong>Cost:</strong> cursors hold a transaction open; mitigated by an idle timeout and an explicit &quot;cursor open&quot; indicator.</p><h2 id="adr-009-·-queryhost-child-process-deferred-to-phase-8-interface-ready-from-phase-1" tabindex="-1">ADR-009 · <code>QueryHost</code> child process deferred to Phase 8, interface-ready from Phase 1 <a class="header-anchor" href="#adr-009-·-queryhost-child-process-deferred-to-phase-8-interface-ready-from-phase-1" aria-label="Permalink to &quot;ADR-009 · `QueryHost` child process deferred to Phase 8, interface-ready from Phase 1&quot;">​</a></h2><p><strong>Decided.</strong><strong>Why:</strong> in-process is simpler and adequate through beta; the <code>Session</code> interface is process-agnostic from the start, so the move is transport work, not a redesign.</p><h2 id="adr-010-·-mit-license-open-core-no-paywalled-features" tabindex="-1">ADR-010 · MIT license, open core, no paywalled features <a class="header-anchor" href="#adr-010-·-mit-license-open-core-no-paywalled-features" aria-label="Permalink to &quot;ADR-010 · MIT license, open core, no paywalled features&quot;">​</a></h2><p><strong>Decided.</strong><strong>Why:</strong> the freemium extensions are the main thing users complain about. Differentiation is quality, not feature gating. Sustainability, if needed later, comes from optional paid <em>team</em> services (shared connection catalogs, audit) — never from crippling the client.</p><h2 id="adr-011-·-publish-to-both-marketplace-and-open-vsx" tabindex="-1">ADR-011 · Publish to both Marketplace and Open VSX <a class="header-anchor" href="#adr-011-·-publish-to-both-marketplace-and-open-vsx" aria-label="Permalink to &quot;ADR-011 · Publish to both Marketplace and Open VSX&quot;">​</a></h2><p><strong>Decided.</strong> Avoid VS Code-proprietary APIs so Cursor / Windsurf / VSCodium work from the same VSIX. <strong>Cost:</strong> near zero. Reach: large.</p><h2 id="adr-012-·-pure-js-runtime-dependencies-no-native-modules" tabindex="-1">ADR-012 · Pure-JS runtime dependencies; no native modules <a class="header-anchor" href="#adr-012-·-pure-js-runtime-dependencies-no-native-modules" aria-label="Permalink to &quot;ADR-012 · Pure-JS runtime dependencies; no native modules&quot;">​</a></h2><p><strong>Decided.</strong><strong>Why:</strong> native modules force per-platform VSIX builds and break in some remote/devcontainer images. <code>pg</code> is pure JS; <code>ssh2</code>&#39;s native crypto is optional with a JS fallback.</p><h2 id="adr-013-prefer-and-allow-are-implemented-as-real-negotiating-modes" tabindex="-1">ADR-013 - <code>prefer</code> and <code>allow</code> are implemented as real negotiating modes <a class="header-anchor" href="#adr-013-prefer-and-allow-are-implemented-as-real-negotiating-modes" aria-label="Permalink to &quot;ADR-013 - `prefer` and `allow` are implemented as real negotiating modes&quot;">​</a></h2><p><strong>Decided.</strong> For <code>sslmode=prefer</code> Pitta attempts TLS and, on a TLS-negotiation failure specifically, retries in plaintext; <code>allow</code> does the reverse. Every other mode is absolute and never falls back. <strong>Why:</strong> node-postgres has no notion of a negotiating mode - handed an ssl option it fails outright against a server built without TLS, which is most local development installs. Without this, <code>prefer</code> behaved as <code>require</code> and Postgres.app / a default Homebrew server could not be connected to at all. Found by the integration suite on the first real run. <strong>Guard:</strong> the fallback triggers only on messages that indicate TLS negotiation (&quot;does not support SSL&quot;, &quot;SSL off&quot;). An authentication failure must never cause a plaintext retry, or a password would be re-sent in the clear. Unit-tested explicitly.</p><h2 id="adr-014-ddl-is-a-virtual-read-only-document-not-a-panel-tab" tabindex="-1">ADR-014 - DDL is a virtual read-only document, not a panel tab <a class="header-anchor" href="#adr-014-ddl-is-a-virtual-read-only-document-not-a-panel-tab" aria-label="Permalink to &quot;ADR-014 - DDL is a virtual read-only document, not a panel tab&quot;">​</a></h2><p><strong>Decided.</strong> <code>pitta-ddl:</code> scheme + <code>TextDocumentContentProvider</code>, opened as a <code>sql</code> document. <strong>Why:</strong> syntax highlighting, find, folding, copy, and Save As all come free and behave the way the user already expects. A webview tab would reimplement each of them worse, and the output is plain text with no interaction to speak of. <strong>Consequence:</strong> the Structure panel (P3-T9) loses its DDL tab; the tree already exposes columns, indexes, constraints, triggers, and policies as child nodes, so what remains for that panel is a consolidated view rather than new data.</p><h2 id="adr-015-a-failed-statement-stops-the-batch" tabindex="-1">ADR-015 - A failed statement stops the batch <a class="header-anchor" href="#adr-015-a-failed-statement-stops-the-batch" aria-label="Permalink to &quot;ADR-015 - A failed statement stops the batch&quot;">​</a></h2><p><strong>Decided.</strong> When one statement in a run fails, the rest are marked skipped rather than attempted. <strong>Why:</strong> the statements in a file are usually a sequence, not a set. Running statement 5 after statement 4 failed executes it against a state the author did not intend. Skipped statements are shown as skipped, so nothing is silent.</p><h2 id="adr-016-an-editor-binding-names-a-database-not-just-a-connection" tabindex="-1">ADR-016 - An editor binding names a database, not just a connection <a class="header-anchor" href="#adr-016-an-editor-binding-names-a-database-not-just-a-connection" aria-label="Permalink to &quot;ADR-016 - An editor binding names a database, not just a connection&quot;">​</a></h2><p><strong>Decided.</strong> A <code>.sql</code> file is bound to <code>(connection, database)</code>. The status bar always shows both, and &quot;New Query&quot; from a database node binds to that database. <strong>Why:</strong> Postgres cannot query across databases, so a connection alone does not say where a statement lands. A file bound only to <code>orders-prod</code> still has to choose between <code>orders</code> and <code>analytics</code>, and choosing wrong is a <em>silent</em> failure - the statement runs, against the wrong data. Naming both makes the target unambiguous everywhere it is displayed, including the production confirmation prompt. <strong>Directives:</strong> <code>-- @pitta connection: name</code> and <code>-- @pitta database: db</code>. Either pins; both travel with a checked-in file.</p><h2 id="adr-017-the-schema-graph-is-read-in-two-queries-not-two-per-table" tabindex="-1">ADR-017 - The schema graph is read in two queries, not two per table <a class="header-anchor" href="#adr-017-the-schema-graph-is-read-in-two-queries-not-two-per-table" aria-label="Permalink to &quot;ADR-017 - The schema graph is read in two queries, not two per table&quot;">​</a></h2><p><strong>Decided.</strong> <code>readSchemaGraph</code> issues one query for relations (columns packed per row) and one for foreign keys. <strong>Why:</strong> a 300-table schema drawn by introspecting each relation individually is 300 round trips for one picture, and many tables is exactly the case where a diagram earns its keep. Packing columns into one array per relation, separated by ASCII 31, keeps the result set narrow enough to draw in one pass.</p><h2 id="adr-018-the-diagram-is-svg-with-resolved-colors-not-canvas" tabindex="-1">ADR-018 - The diagram is SVG with resolved colors, not canvas <a class="header-anchor" href="#adr-018-the-diagram-is-svg-with-resolved-colors-not-canvas" aria-label="Permalink to &quot;ADR-018 - The diagram is SVG with resolved colors, not canvas&quot;">​</a></h2><p><strong>Decided.</strong> Tables and edges are SVG elements; colors are resolved from the theme variables into concrete values before drawing. <strong>Why:</strong> SVG gives hit-testing, text selection, and accessible labels for free, and the same element tree is what gets exported. Concrete colors are what make the export usable - a file full of <code>var(--vscode-...)</code> renders blank outside the editor. The palette is re-resolved when the theme changes, so the on-screen diagram still follows light/dark.</p><h2 id="adr-019-a-generated-migration-is-a-sql-document-applied-through-query-mode" tabindex="-1">ADR-019 - A generated migration is a SQL document, applied through query mode <a class="header-anchor" href="#adr-019-a-generated-migration-is-a-sql-document-applied-through-query-mode" aria-label="Permalink to &quot;ADR-019 - A generated migration is a SQL document, applied through query mode&quot;">​</a></h2><p><strong>Decided.</strong> Schema comparison produces a <code>pitta-migration:</code> virtual document rather than a bespoke review panel with its own Apply button. <strong>Why:</strong> the reviewer gets highlighting, find, folding and Save As for free, and running it goes through query mode - which already has the destructive-statement guards, the typed production confirmation, per-statement results, and real cancellation. A separate apply path would have had to reimplement all of that, and would have been the weaker of the two. <strong>Consequence:</strong> applying is deliberately two steps (generate, then run). That is the right friction for a schema migration.</p><h2 id="adr-020-definitions-are-compared-with-the-schema-qualifier-removed" tabindex="-1">ADR-020 - Definitions are compared with the schema qualifier removed <a class="header-anchor" href="#adr-020-definitions-are-compared-with-the-schema-qualifier-removed" aria-label="Permalink to &quot;ADR-020 - Definitions are compared with the schema qualifier removed&quot;">​</a></h2><p><strong>Decided.</strong> Constraint, index, view, routine and user-defined column types are normalized by stripping their own schema name before comparison, and the renderer can retarget the qualifier baked into server-rendered definitions. <strong>Why:</strong> <code>pg_get_constraintdef</code> renders <code>REFERENCES v1.parent(id)</code>. Comparing <code>v1</code> against <code>v2</code> verbatim reports every foreign key, index, view and enum column as changed - forever. The generated migration would drop and re-add them on every run and never converge. Caught by the round-trip integration tests, which apply each migration and re-diff expecting nothing.</p><h2 id="adr-021-adr-001-held-for-the-driver-but-the-host-had-leaked" tabindex="-1">ADR-021 - ADR-001 held for the driver, but the host had leaked <a class="header-anchor" href="#adr-021-adr-001-held-for-the-driver-but-the-host-had-leaked" aria-label="Permalink to &quot;ADR-021 - ADR-001 held for the driver, but the host had leaked&quot;">​</a></h2><p><strong>Finding.</strong> Adding SQLite needed no change to <code>packages/core</code> for the <em>driver itself</em>: capabilities, introspection, SQL building, types and DDL all fit the existing contract, and the same conformance kit passes against both engines.</p><p>But the audit (P9-T3) found three places where the extension host had quietly assumed Postgres:</p><ol><li><code>TableDataService.estimateRows</code> ran <code>pg_catalog.pg_class</code> SQL directly. Fixed by adding an optional <code>estimateRowCount</code> to <code>DriverAdapter</code> - a real contract addition, so the strict &quot;no core change&quot; form of ADR-001 was not met.</li><li>The exact-count query used a <code>::text</code> cast, which SQLite rejects.</li><li>The blank connection form hardcoded host/port/database defaults, which are meaningless for a file-backed engine. Defaults now come from the driver&#39;s own <code>connectionSchema</code>.</li></ol><p><strong>Why it is worth recording as a finding rather than quietly fixing.</strong> The abstraction was only ever tested against one engine, and single-engine abstractions drift without anyone noticing. CI now fails a PR that touches both <code>packages/driver-*</code> and <code>packages/core/src</code> unless it carries a <code>contract-change</code> label, so the next drift is a deliberate decision.</p><h2 id="adr-022-sqlite-ships-as-webassembly-not-a-native-binding" tabindex="-1">ADR-022 - SQLite ships as WebAssembly, not a native binding <a class="header-anchor" href="#adr-022-sqlite-ships-as-webassembly-not-a-native-binding" aria-label="Permalink to &quot;ADR-022 - SQLite ships as WebAssembly, not a native binding&quot;">​</a></h2><p><strong>Decided.</strong> <code>node-sqlite3-wasm</code>, vendored next to the bundle at build time. <strong>Why:</strong> ADR-012 rules out native modules - they force a per-platform VSIX and break in some remote and devcontainer images. <code>node:sqlite</code> would be simpler, but VS Code&#39;s Electron ships Node 20 and that module landed in Node 22. <strong>Cost:</strong> ~1.3MB of WASM, loaded lazily on first SQLite connection so a user who never opens one does not pay for it at activation.</p><h2 id="adr-023-two-performance-tasks-cancelled-by-measurement" tabindex="-1">ADR-023 - Two performance tasks cancelled by measurement <a class="header-anchor" href="#adr-023-two-performance-tasks-cancelled-by-measurement" aria-label="Permalink to &quot;ADR-023 - Two performance tasks cancelled by measurement&quot;">​</a></h2><p><strong>Decided.</strong> P8-T1 (a forked QueryHost child process) and P8-T2 (a columnar store with typed arrays and string interning) are not being built.</p><p><strong>Why.</strong> Both were planned before anything was measured. The benchmark suite now says:</p><table tabindex="0"><thead><tr><th>Measurement</th><th>Result</th><th>Budget</th></tr></thead><tbody><tr><td>Stream 1M rows x 20 columns</td><td>3.1 s, peak <strong>+10 MB</strong></td><td>600 MB</td></tr><tr><td>Memory vs result size</td><td>50k: +3.5 MB · 500k: +2.0 MB</td><td>flat</td></tr><tr><td>First page of 1M rows</td><td><strong>55 ms</strong></td><td>500 ms</td></tr><tr><td>First page, 10k vs 2M rows</td><td>2 ms vs 97 ms</td><td>independent of size</td></tr><tr><td>Activation, cold bundle load</td><td><strong>28 ms</strong></td><td>200 ms</td></tr><tr><td>1000 queries opened and disposed</td><td>+14.6 MB</td><td>no leak</td></tr><tr><td>Cell encoding</td><td>48M cells/s</td><td>-</td></tr></tbody></table><p>P8-T1 existed to stop a large result exhausting the extension host. A million rows costs ten megabytes, so the problem it solves does not occur; a child process and a pipe transport would be real complexity bought for nothing.</p><p>P8-T2 existed to reduce grid memory. Cells already cross the boundary as text, the panel holds one page, and encoding is not close to being a bottleneck.</p><p>If a future workload contradicts these numbers the benchmarks will say so, because they now run in CI as assertions rather than as a report nobody reads.</p><p><strong>What measurement did change:</strong> <code>pitta.maxRowsInMemory</code> defaulted to 200,000, which costs <strong>+108.8 MB</strong> on a 20-column result. Lowered to 50,000 - about 27 MB - because that ceiling exists to bound an accidental <code>SELECT *</code>, and anyone who genuinely wants a larger result can raise it.</p><p><strong>Still open:</strong> P8-T3. The runner buffers up to the cap before the panel sees anything, so a large result is held in the host even though only a page is displayed. The lower default reduces the cost; it does not remove it.</p><h2 id="adr-024-plan-findings-carry-advice-not-just-labels" tabindex="-1">ADR-024 - Plan findings carry advice, not just labels <a class="header-anchor" href="#adr-024-plan-findings-carry-advice-not-just-labels" aria-label="Permalink to &quot;ADR-024 - Plan findings carry advice, not just labels&quot;">​</a></h2><p><strong>Decided.</strong> Every red-flag rule states what it observed, in numbers, and what that usually means. <strong>Why:</strong> a visualizer that only draws the tree leaves the reader to know what to look for, and &quot;Seq Scan&quot; on its own is not a problem - on a forty-row table it is the right plan. The rules therefore measure rows <em>scanned</em> rather than rows returned, weigh a filter&#39;s selectivity, and account for loops, so that what is reported is a finding rather than a node type.</p><p>Caught by testing against a real planner: the sequential-scan rule originally counted rows returned, so a scan reading 100,000 rows and returning none - the case most worth flagging - was silently missed.</p><h2 id="adr-025-session-monitoring-goes-through-the-driver-contract" tabindex="-1">ADR-025 - Session monitoring goes through the driver contract <a class="header-anchor" href="#adr-025-session-monitoring-goes-through-the-driver-contract" aria-label="Permalink to &quot;ADR-025 - Session monitoring goes through the driver contract&quot;">​</a></h2><p><strong>Decided.</strong> <code>readActivity</code>, <code>stopBackend</code> and <code>readHealth</code> are optional methods on <code>DriverAdapter</code>, behind <code>supportsSessionMonitor</code> and <code>supportsHealthReport</code>. <strong>Why:</strong> the first draft of the panel imported <code>@pitta/driver-pg</code> directly, which is precisely the leak ADR-021 had just been written about. SQLite has no concurrent sessions to inspect, declares both flags false, and the command declines instead of failing. <strong>Also:</strong> nothing is polled by default. A monitor sampling <code>pg_stat_activity</code> every second is itself load on the server it is watching, so refresh is manual with an opt-in five-second auto mode.</p><hr><h3 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h3><ul><li><strong>OQ-1</strong> Publisher name and final extension name (<code>pitta-db</code> is a working title; check Marketplace availability).</li><li><strong>OQ-2</strong> Notebook file extension — <code>.psql-nb</code> vs reusing <code>.ipynb</code> with a SQL kernel.</li><li><strong>OQ-3</strong> Do we ship the optional DDL event-trigger for cross-client cache invalidation, or is asking users to install a trigger too invasive? (Leaning: ship it, off by default, documented.)</li><li><strong>OQ-4</strong> <s>Second driver: SQLite vs MySQL.</s> <strong>Resolved</strong> by ADR-047: SQLite shipped in Phase 9; the order after it is contract generalization (Phase 11), MongoDB (12), Redis (13), MySQL/MariaDB (14).</li><li><strong>OQ-5</strong> MongoDB Atlas Search and vector search: a separate tree branch and a query surface, or out of scope for a client? (Leaning: list the indexes in Phase 12, defer the search UI.)</li><li><strong>OQ-6</strong> Redis modules — JSON, Time Series, Search, Bloom, vector sets. JSON is in for Phase 13 because it changes how a key is <em>displayed</em>. The others are detected and listed; whether any gets a real view is a post-13 question.</li><li><strong>OQ-7</strong> Which engines a <code>.pittabook</code> may mix. ADR-029 binds a notebook to one database, which already answers it for now, but a &quot;read from Postgres, write to Redis&quot; notebook is the obvious next request and the answer is currently no (ADR-048).</li><li><strong>OQ-8</strong> Whether Amazon DocumentDB, Azure Cosmos DB&#39;s Mongo API, Valkey and Dragonfly are claimed as supported. Each answers a subset of the wire protocol. Position: they are not claimed until they run in CI (the <code>03 §6</code> rule), but the driver should not go out of its way to break on them.</li></ul><h2 id="adr-026-the-postgres-grammar-ships-as-webassembly-vendored-beside-the-bundle" tabindex="-1">ADR-026 - The Postgres grammar ships as WebAssembly, vendored beside the bundle <a class="header-anchor" href="#adr-026-the-postgres-grammar-ships-as-webassembly-vendored-beside-the-bundle" aria-label="Permalink to &quot;ADR-026 - The Postgres grammar ships as WebAssembly, vendored beside the bundle&quot;">​</a></h2><p><strong>Context.</strong> ADR-007 promised the Phase 2 heuristic classifier would be replaced by a real parse tree. The obvious candidate, <code>libpg-query</code>, is a native addon: it would mean a per-platform VSIX and a compiler on the user&#39;s machine, which ADR-012 rules out.</p><p><strong>Decision.</strong> Use <code>@pgsql/parser</code>, which is the same Postgres grammar compiled to WebAssembly. The v17 grammar is vendored into <code>dist/vendor/pgsql-parser/</code> by the build and loaded lazily through <code>createRequire</code>, first from the vendored path and then from node_modules, so the same code works from source and from a VSIX.</p><p>Only v17 is shipped. The package carries v13 through v18 and each has its own 1.1 MB <code>.wasm</code>; v17 accepts the syntax of every version Pitta supports, so the other five would add ~7 MB for grammars nothing loads.</p><p><strong>Consequences.</strong> No native module, one platform-neutral VSIX, and 1.3 MB added to the package. The parser loads when a <code>.sql</code> file first appears rather than at activation - activation stays at 31 ms - and every caller falls back to the heuristic while it is loading.</p><p>Finding this also exposed a real packaging bug: esbuild compiles <code>import.meta.url</code> to an empty object in a CJS bundle, so <em>both</em> WASM loaders - the parser and the SQLite runtime shipped in Phase 9 - were resolving their vendored files against <code>undefined</code>. The build now defines <code>import.meta.url</code> as the bundle&#39;s own path.</p><h2 id="adr-027-the-parse-tree-and-the-heuristic-disagree-toward-asking" tabindex="-1">ADR-027 - The parse tree and the heuristic disagree toward asking <a class="header-anchor" href="#adr-027-the-parse-tree-and-the-heuristic-disagree-toward-asking" aria-label="Permalink to &quot;ADR-027 - The parse tree and the heuristic disagree toward asking&quot;">​</a></h2><p><strong>Context.</strong> With a real parser, the question is what to do when it and the heuristic classifier disagree, and what to do when the parser has not loaded or the statement does not parse.</p><p><strong>Decision.</strong> <code>classifyStatement</code> combines them, and only in one direction: a statement is destructive if <em>either</em> says it is. The parse tree wins on what a statement <em>is</em> - its kind, its relations, its aliases - but it can never downgrade a destructive verdict. A statement that does not parse keeps the heuristic&#39;s answer rather than being treated as safe, because a syntax error says nothing about whether the text would have dropped a table.</p><p>Semantic diagnostics inherit the same caution. Syntax errors are reported as errors, because the server&#39;s own grammar rejected the text. Unknown names are warnings, are suppressed entirely when the symbol index is empty, and only cover relations and <em>qualified</em> columns - an unqualified name has too many legitimate ways to resolve (CTE output, a function&#39;s columns, an alias defined later) for a warning to be trustworthy.</p><p><strong>Consequences.</strong> Guards can never be weakened by a parser bug, only strengthened. The cost is that a heuristic false positive survives the parse tree; measured against a set of statements the heuristic gets right, that has not been observed, and the failure it prevents is the expensive one.</p><h2 id="adr-028-saved-queries-are-files-history-is-not" tabindex="-1">ADR-028 - Saved queries are files, history is not <a class="header-anchor" href="#adr-028-saved-queries-are-files-history-is-not" aria-label="Permalink to &quot;ADR-028 - Saved queries are files, history is not&quot;">​</a></h2><p><strong>Context.</strong> Phase 5 adds two ways to keep a statement around, and they pull in opposite directions. A saved query is something a team curates. History is a byproduct of working.</p><p><strong>Decision.</strong> Saved queries are ordinary <code>.sql</code> files in a workspace folder (<code>.pitta/queries</code> by default), so they are reviewed, diffed, and versioned like any other file, and subdirectories become groups in the tree. History is JSONL in the extension&#39;s global storage, is not shared, and <strong>excludes production by default</strong> - it is a durable record of statements run against real databases, so what it omits is part of the design (S6).</p><p>Saved queries may take <code>:name</code> parameters. Those are rewritten to the engine&#39;s positional placeholders through <code>driver.placeholder</code>, and the values are bound, never interpolated - a saved query cannot be turned into an injection by what someone types at the prompt (S3).</p><p><strong>Consequences.</strong> A team shares saved queries through their repository and nothing else. History stays local and stays out of production by default; a team that wants production recorded has to say so explicitly.</p><h2 id="adr-029-a-notebook-is-bound-to-one-database-and-saves-as-runnable-sql" tabindex="-1">ADR-029 - A notebook is bound to one database, and saves as runnable SQL <a class="header-anchor" href="#adr-029-a-notebook-is-bound-to-one-database-and-saves-as-runnable-sql" aria-label="Permalink to &quot;ADR-029 - A notebook is bound to one database, and saves as runnable SQL&quot;">​</a></h2><p><strong>Context.</strong> Notebooks could bind per cell, and could serialize to JSON with their outputs like Jupyter.</p><p><strong>Decision.</strong> Neither. The binding is per notebook, because a notebook is a narrative about one database and a cell that quietly ran somewhere else makes the narrative false. The file is a runnable <code>.sql</code> script, with cell boundaries and markdown carried in comments the parser ignores, so a notebook someone commits reads well in a diff and still runs under psql.</p><p>Outputs are never saved. A result set is a snapshot of a database at one moment, and writing it into a file that looks like a script invites the next reader to take stale numbers as current.</p><p><strong>Consequences.</strong> A <code>.pittabook</code> is diffable and portable, and opening a plain <code>.sql</code> file as a notebook works - it is split into one cell per statement. The cost is that reopening a notebook shows no results until the cells are run again, which is the intended trade.</p><h2 id="adr-030-bulk-import-goes-through-the-driver-and-validates-before-it-writes" tabindex="-1">ADR-030 - Bulk import goes through the driver, and validates before it writes <a class="header-anchor" href="#adr-030-bulk-import-goes-through-the-driver-and-validates-before-it-writes" aria-label="Permalink to &quot;ADR-030 - Bulk import goes through the driver, and validates before it writes&quot;">​</a></h2><p><strong>Context.</strong> Importing a CSV with a loop of INSERTs is slow enough to make the feature useless on the files people actually import, and Postgres has COPY. COPY is a protocol mode rather than a statement, so it does not fit the QueryHandle contract.</p><p><strong>Decision.</strong> <code>bulkLoad</code> on the driver contract, gated on a new <code>supportsBulkLoad</code> capability, taking rows as an async iterable so a 2 GB file never becomes 2 GB of memory (ADR-008). PostgreSQL implements it with <code>COPY ... FROM STDIN</code>; an engine without a bulk path falls back to batched multi-row INSERTs inside one transaction, so a failure halfway through leaves the table unchanged either way.</p><p>Coercion runs <em>before</em> anything is sent, on a capped sample, and the failures are shown as a report. Learning that column 7 contains <code>N/A</code> after 400,000 rows have been committed is the failure this exists to prevent. Numeric values are sent as text, never through a JS number, on the way in as well as out (ADR-004).</p><p><strong>Consequences.</strong> One new capability flag and one new optional contract method, which is the additive shape ADR-001 asks for. COPY&#39;s text format means escaping is Pitta&#39;s responsibility: an unescaped tab does not produce a bad row, it shifts every following column, so that escaping is tested directly.</p><h2 id="adr-031-nothing-hand-written-may-live-in-the-webview-output-directory" tabindex="-1">ADR-031 - Nothing hand-written may live in the webview output directory <a class="header-anchor" href="#adr-031-nothing-hand-written-may-live-in-the-webview-output-directory" aria-label="Permalink to &quot;ADR-031 - Nothing hand-written may live in the webview output directory&quot;">​</a></h2><p><strong>Context.</strong> The walkthrough pages were written into <code>packages/extension/media/walkthrough/</code>. The Vite build writes webview bundles into <code>packages/extension/media</code> with <code>emptyOutDir: true</code>, so the next build deleted them. Everything still compiled, every test still passed, and the VSIX shipped without the files - a failure visible only to a user.</p><p><strong>Decision.</strong> Anything the extension ships that is not a webview build output lives in its own directory beside <code>media/</code> - <code>walkthrough/</code>, <code>snippets/</code>, <code>resources/</code>. A manifest test asserts that every file the manifest points at exists on disk and is not under <code>media/</code>, and the release workflow greps the built VSIX for the files that are loaded at runtime rather than imported.</p><p><strong>Consequences.</strong> The class of bug where a build silently removes a shipped asset now fails a test rather than reaching a user. The same VSIX check also covers the two WASM runtimes, which are loaded from disk and would otherwise fail only at runtime.</p><h2 id="adr-032-the-structure-view-is-a-list-of-sections-not-a-fixed-set-of-tabs" tabindex="-1">ADR-032 - The structure view is a list of sections, not a fixed set of tabs <a class="header-anchor" href="#adr-032-the-structure-view-is-a-list-of-sections-not-a-fixed-set-of-tabs" aria-label="Permalink to &quot;ADR-032 - The structure view is a list of sections, not a fixed set of tabs&quot;">​</a></h2><p><strong>Context.</strong> The structure view shows what a table has besides rows: columns, indexes, constraints, triggers, policies, partitions, statistics. The obvious implementation is a typed record per section and a tab component that knows all seven.</p><p><strong>Decision.</strong> The driver returns <code>StructureSection[]</code> - each a label, a list of column descriptors, and rows of text - and the panel renders whatever it was given, in the order core sorts them into. Which sections exist is an engine decision: PostgreSQL has row-level policies and declarative partitions, SQLite has neither, and a core type enumerating PostgreSQL&#39;s catalog would be exactly the leak ADR-001 exists to prevent. A section that does not apply is omitted rather than returned empty, because a tab that always reads &quot;none&quot; is worse than no tab.</p><p>Every value crosses as text, sizes and tuple counts included, for the same reason rows do (ADR-004).</p><p><strong>Consequences.</strong> Adding a section is a change in one driver file. The cost is that the panel cannot do anything clever per section - it cannot, say, offer &quot;drop this index&quot; from the indexes tab without the driver saying which rows are droppable. That is a real limit and the right time to lift it is when there is a second engine to check the design against.</p><h2 id="adr-033-the-detail-pane-renders-by-content-and-always-keeps-the-raw-text" tabindex="-1">ADR-033 - The detail pane renders by content, and always keeps the raw text <a class="header-anchor" href="#adr-033-the-detail-pane-renders-by-content-and-always-keeps-the-raw-text" aria-label="Permalink to &quot;ADR-033 - The detail pane renders by content, and always keeps the raw text&quot;">​</a></h2><p><strong>Context.</strong> A grid cell is one line high. A 40 KB description, a nested JSON document, and a PNG in a <code>bytea</code> column are all unreadable in it.</p><p><strong>Decision.</strong> The pane offers a view per shape - JSON tree, image, hex dump, text - chosen from the column&#39;s kind <em>and the value&#39;s own bytes</em>. A <code>bytea</code> column says nothing about what someone put in it, so an image is recognised by its magic bytes rather than by the column&#39;s name or type, and a PDF in an <code>avatar</code> column simply gets the hex view.</p><p>The raw text view is always available and always last in the list. A rendered view that quietly differs from what is stored would be worse than no rendering, and the raw view is what settles the question.</p><p>Images render from a <code>data:</code> URI built in the webview from the hex Postgres already sent, so nothing is fetched and the CSP stays closed (S5.1).</p><p><strong>Consequences.</strong> The decision logic lives in a React-free module and is tested directly - &quot;is this hex&quot;, &quot;is this a PNG&quot;, &quot;should JSON be offered&quot; are exactly the parts that break quietly.</p><h2 id="adr-034-pitta-is-a-multi-model-client-and-the-contract-stops-assuming-sql" tabindex="-1">ADR-034 - Pitta is a multi-model client, and the contract stops assuming SQL <a class="header-anchor" href="#adr-034-pitta-is-a-multi-model-client-and-the-contract-stops-assuming-sql" aria-label="Permalink to &quot;ADR-034 - Pitta is a multi-model client, and the contract stops assuming SQL&quot;">​</a></h2><p><strong>Context.</strong> ADR-001 built a driver registry so a second engine would not be a rewrite, and Phase 9 proved it for SQLite. SQLite is relational and speaks SQL, so what it proved is narrower than it looked: the contract still assumes a statement is SQL text, a result is columns by rows, introspection is a query, and the tree vocabulary is a closed relational enum. MongoDB and Redis break all four.</p><p><strong>Decided.</strong> Generalize the contract deliberately, as one phase, before either engine is written - <code>docs/10-ENGINE-MODEL.md</code> is the delta. A driver declares its language (<code>sql</code>, <code>mongodb</code>, <code>redis</code>); <code>Statement.sql</code> becomes <code>Statement.text</code> and is engine-native; splitting, formatting and classification move behind optional driver methods; results declare a shape; the tree vocabulary opens up.</p><p><strong>Why now rather than per-engine.</strong> Doing it inside the MongoDB PR would make every core change look like a MongoDB detail and get reviewed as one. Doing it after Redis would mean two rounds of the same argument. ADR-021 already established that a core change forced by a driver is a decision that needs a label on the PR; this is that decision made once, in the open, with the two engines that motivate each part named beside it.</p><p><strong>Consequences.</strong> Roughly three weeks of work that ships no user-visible feature, and one mechanical rename across every driver. In exchange, the MongoDB and Redis PRs are additive in the way ADR-001 promised, and a fourth engine after them is a package again. The alternative - a <code>mongo</code> fork of the extension - is the outcome this project exists to avoid.</p><h2 id="adr-035-capabilities-are-static-then-refined-by-the-session" tabindex="-1">ADR-035 - Capabilities are static, then refined by the session <a class="header-anchor" href="#adr-035-capabilities-are-static-then-refined-by-the-session" aria-label="Permalink to &quot;ADR-035 - Capabilities are static, then refined by the session&quot;">​</a></h2><p><strong>Context.</strong> <code>DriverCapabilities</code> is a constant per driver. It cannot be. Mongo has multi-document transactions only on a replica set or sharded cluster, Redis in cluster mode cannot run a cross-slot <code>MULTI</code>, and a Redis connection under an ACL user may be unable to run <code>CLIENT LIST</code> at all - the same server, two users, two different sets of true statements.</p><p><strong>Decided.</strong> <code>Session</code> may carry <code>capabilities?: Partial&lt;DriverCapabilities&gt;</code>. Core resolves the static flags overlaid with the session&#39;s, and every UI gate reads the resolved value.</p><p><strong>Why not just widen the static flags.</strong> Claiming transactions and failing at apply time is exactly the &quot;offer it and fail&quot; behavior ADR-001&#39;s flags exist to prevent. Claiming they are absent everywhere would take real atomicity away from the majority of MongoDB deployments, which are replica sets.</p><p><strong>Consequences.</strong> One resolution point in core, and a rule: a driver may only narrow at the session level, never claim something its static flags deny. Also retires a small Postgres wart - version-gated feature checks that currently live in call sites can move into the session&#39;s overlay.</p><h2 id="adr-036-objectkind-becomes-an-open-vocabulary" tabindex="-1">ADR-036 - <code>ObjectKind</code> becomes an open vocabulary <a class="header-anchor" href="#adr-036-objectkind-becomes-an-open-vocabulary" aria-label="Permalink to &quot;ADR-036 - `ObjectKind` becomes an open vocabulary&quot;">​</a></h2><p><strong>Decided.</strong> <code>ObjectKind</code> is <code>string</code>. The relational kinds stay as exported constants and keep their shared behavior; a driver declares anything else through <code>nodeKinds: NodeKindDescriptor[]</code> - label, plural, icon, and whether the node can open a data or structure view.</p><p><strong>Why.</strong> The alternative is extending a core enum for every engine forever: <code>collection</code>, <code>key</code>, <code>keyspace</code>, <code>stream</code>, <code>consumerGroup</code> now, and whatever the engine after that has. That makes every new engine a core edit, which is the condition ADR-001 was written to remove.</p><p><strong>Cost.</strong> The compiler stops catching a typo&#39;d kind. Mitigated by the conformance kit asserting that every kind a driver&#39;s introspection plan emits has a descriptor, and that every descriptor names a real codicon.</p><h2 id="adr-037-introspection-may-be-imperative-and-the-tree-pages" tabindex="-1">ADR-037 - Introspection may be imperative, and the tree pages <a class="header-anchor" href="#adr-037-introspection-may-be-imperative-and-the-tree-pages" aria-label="Permalink to &quot;ADR-037 - Introspection may be imperative, and the tree pages&quot;">​</a></h2><p><strong>Decided.</strong> <code>NodeDefinition</code> gains <code>fetch(session, context)</code> as an alternative to <code>query()</code> + <code>map()</code>, returning nodes and an optional cursor. The tree gains a <strong>Load more</strong> node and <code>pitta.explorer.treePageSize</code>.</p><p><strong>Why.</strong> Redis has no catalog to query - listing keys is a <code>SCAN</code> cursor loop - and MongoDB&#39;s catalog is a command, not a query. Forcing either into &quot;return SQL text and core will run it&quot; would mean inventing a fake query language for the driver to speak to itself.</p><p>Paging is not cosmetic. A Redis database holds millions of keys with no <code>LIMIT</code>; without a cursor in the contract, expanding a database node is either wrong or an outage. It also caps the MongoDB database with twenty thousand collections, and the Postgres schema with ten thousand tables, which the tree currently loads in full.</p><p><strong>Consequences.</strong> Core keeps caching, version gating and invalidation, because <code>fetch</code> returns the same <code>TreeNodeData</code>. Drivers must not implement both forms for one node; the kit checks it.</p><h2 id="adr-038-a-result-declares-its-shape-and-documents-are-a-sampled-projection" tabindex="-1">ADR-038 - A result declares its shape, and documents are a sampled projection <a class="header-anchor" href="#adr-038-a-result-declares-its-shape-and-documents-are-a-sampled-projection" aria-label="Permalink to &quot;ADR-038 - A result declares its shape, and documents are a sampled projection&quot;">​</a></h2><p><strong>Decided.</strong> Results carry a <code>ResultShape</code> - <code>rows</code>, <code>documents</code>, <code>keyValue</code>, <code>message</code>, <code>none</code>. A <code>documents</code> result gets its columns from a sample of the fetched page (default 200), <code>_id</code> first then by frequency, with each field marked <code>inferred</code>. <code>message</code> replies render as a message, not as a one-cell grid.</p><p><strong>Why.</strong> A grid needs columns and a collection does not have any. Sampling is the only honest way to get them, and saying so in the field metadata is what lets the UI label the header rather than implying the server declared it.</p><p>The raw document is always in the detail pane and always the last view offered, which is ADR-033&#39;s rule one level up: a projection that quietly differs from what is stored would be worse than no projection, and the raw view is what settles the question.</p><p><strong>Consequences.</strong> Two documents with disjoint keys produce a wide, sparse grid - correct, and occasionally ugly. The column set can change as you page; the grid keeps columns it has seen and marks newly-absent ones rather than reshuffling under the cursor.</p><h2 id="adr-039-missing-is-not-null" tabindex="-1">ADR-039 - Missing is not null <a class="header-anchor" href="#adr-039-missing-is-not-null" aria-label="Permalink to &quot;ADR-039 - Missing is not null&quot;">​</a></h2><p><strong>Decided.</strong> The wire cell type becomes <code>string | null | { missing: true }</code>. The grid renders a distinct <code>MISSING</code> token. Filters offer <code>exists</code> / <code>does not exist</code> alongside <code>IS NULL</code>. Clearing a field and removing a field are two different edit actions producing <code>$set: null</code> and <code>$unset</code>. Engines that cannot tell the difference declare <code>distinguishesMissing: false</code> and never see it.</p><p><strong>Why.</strong> In a document store <code>{ a: null }</code> and <code>{}</code> are different documents that different queries match, and merging them is the same class of error as rendering <code>NULL</code> as an empty string - which ADR-004 already refuses to do. A client that cannot express &quot;remove this field&quot; cannot fix a document that has a field it should not.</p><p><strong>Cost.</strong> A third state in the wire schema, the grid renderer, the filter row and every cell editor. It is a small amount of work in a lot of places, which is why it is decided before either driver is written rather than after.</p><h2 id="adr-040-guards-classify-from-the-engine-s-own-metadata-and-unknown-fails-closed-on-production" tabindex="-1">ADR-040 - Guards classify from the engine&#39;s own metadata, and unknown fails closed on production <a class="header-anchor" href="#adr-040-guards-classify-from-the-engine-s-own-metadata-and-unknown-fails-closed-on-production" aria-label="Permalink to &quot;ADR-040 - Guards classify from the engine&#39;s own metadata, and unknown fails closed on production&quot;">​</a></h2><p><strong>Decided.</strong> <code>classifyCommand(text, session)</code> on the driver returns kind, destructive, unfiltered, targets and a human reason. ADR-027&#39;s combining rule is unchanged: destructive if <em>either</em> the driver or the shared heuristic says so, and neither can downgrade the other. Two additions:</p><ol><li>Where the engine publishes its own command metadata, that is the source. Redis <code>COMMAND INFO</code> flags every command <code>write</code>, <code>admin</code>, <code>dangerous</code>, <code>blocking</code>; Pitta reads the table at connect, so a module command it has never heard of is still classified correctly. Same principle as ADR-026.</li><li>An <code>unknown</code> classification on a <code>prod</code>-tagged connection is treated as destructive and requires the typed confirmation. On dev it is a single confirm.</li></ol><p><strong>Why the asymmetry with SQL.</strong> A SQL statement that fails to parse keeps the heuristic&#39;s answer because the heuristic is good on SQL. There is no comparable heuristic for an arbitrary Redis module command or an unrecognised <code>db.runCommand</code>, and the failure being guarded against - <code>FLUSHALL</code> on production - takes one line and no time at all.</p><p><strong>Consequences.</strong> Some friction on production against commands that turn out to be harmless. That is the correct direction for the error to point, and the confirmation names what it could not classify so the user can see why they were asked.</p><h2 id="adr-041-atomic-apply-is-a-capability-not-a-promise" tabindex="-1">ADR-041 - Atomic apply is a capability, not a promise <a class="header-anchor" href="#adr-041-atomic-apply-is-a-capability-not-a-promise" aria-label="Permalink to &quot;ADR-041 - Atomic apply is a capability, not a promise&quot;">​</a></h2><p><strong>Decided.</strong> <code>supportsAtomicEdits</code> is a flag, resolved per session (ADR-035). When it is false the apply dialog states, in those words, that changes are applied in order and cannot be rolled back, and that a failure stops the batch leaving earlier changes in place. The result reports exactly which edits were applied.</p><p><strong>Why.</strong> The Postgres data view taught users that Apply is all-or-nothing. Redis <code>MULTI</code>/<code>EXEC</code> does not roll back a command that fails at execution time, and MongoDB has no transaction at all on a standalone server. Carrying the Postgres wording across would be a false guarantee about the user&#39;s data, which is the worst kind to make.</p><p><strong>Consequences.</strong> The affected-count verification of S4.5 degrades where there is no count: it becomes a read-back of the edited records where a key exists, and nothing where it does not - stated in the dialog rather than assumed.</p><h2 id="adr-042-the-mongodb-shell-dialect-is-parsed-never-evaluated" tabindex="-1">ADR-042 - The MongoDB shell dialect is parsed, never evaluated <a class="header-anchor" href="#adr-042-the-mongodb-shell-dialect-is-parsed-never-evaluated" aria-label="Permalink to &quot;ADR-042 - The MongoDB shell dialect is parsed, never evaluated&quot;">​</a></h2><p><strong>Decided.</strong> <code>.mongodb.js</code> text is parsed with <code>acorn</code> to an AST, matched against an allowlist of call shapes, and converted to BSON directly from the syntax tree. Constructor forms (<code>ObjectId</code>, <code>ISODate</code>, <code>NumberLong</code>, <code>NumberDecimal</code>, <code>UUID</code>, <code>BinData</code>, regex literals) are recognised as literals. No <code>eval</code>, no <code>vm</code>, no <code>new Function</code>. Anything outside the allowlist is a diagnostic.</p><p><strong>Why.</strong> The obvious implementation of a mongosh-compatible editor is to run the text as JavaScript. That gives a pasted snippet from a ticket, a wiki, or a chat message full access to the extension host - the file system, the user&#39;s other connections, and their credentials. No sandbox available in a VS Code extension makes that acceptable, and the feature that requires it is loops in a query file.</p><p><strong>Consequences.</strong> No loops, no variables, no user-defined helpers in a query file. The 95% of statements people actually write - <code>find</code>, <code>aggregate</code>, <code>update*</code>, <code>delete*</code>, <code>createIndex</code>, <code>runCommand</code> - are unaffected, and everything else has an error message that says what was rejected rather than failing strangely at runtime.</p><h2 id="adr-043-every-mongodb-operation-carries-a-comment-and-that-is-how-cancel-finds-it" tabindex="-1">ADR-043 - Every MongoDB operation carries a comment, and that is how cancel finds it <a class="header-anchor" href="#adr-043-every-mongodb-operation-carries-a-comment-and-that-is-how-cancel-finds-it" aria-label="Permalink to &quot;ADR-043 - Every MongoDB operation carries a comment, and that is how cancel finds it&quot;">​</a></h2><p><strong>Decided.</strong> Pitta attaches a unique <code>comment</code> to every operation it issues. Cancel closes the cursor and then locates the operation by that comment in <code>$currentOp</code> and issues <code>killOp</code>. The session monitor uses the same field to show which rows are Pitta&#39;s own.</p><p><strong>Why.</strong> Abandoning a cursor does not stop a long-running aggregation on the server. That is the same failure the Postgres out-of-band CancelRequest work (P1-T3) was built to avoid: a Cancel button that only stops the client is a lie about what happened to the server. MongoDB offers no out-of-band cancel channel, and <code>killOp</code> needs an opid, and the only reliable way to know the opid of the operation you just started is to have labelled it.</p><p><strong>Consequences.</strong> Cancel needs a second connection and the <code>killOp</code> privilege; without it the button reports that it stopped reading but could not stop the server, which is at least true. The comment also appears in the profiler and the slow-query log, which turns out to be useful on its own.</p><h2 id="adr-044-the-redis-keyspace-is-browsed-with-scan-and-folders-come-from-a-bounded-sample" tabindex="-1">ADR-044 - The Redis keyspace is browsed with SCAN, and folders come from a bounded sample <a class="header-anchor" href="#adr-044-the-redis-keyspace-is-browsed-with-scan-and-folders-come-from-a-bounded-sample" aria-label="Permalink to &quot;ADR-044 - The Redis keyspace is browsed with SCAN, and folders come from a bounded sample&quot;">​</a></h2><p><strong>Decided.</strong> Every listing is <code>SCAN</code>-based and cursor-paged. <code>KEYS</code> is never issued; the setting that permits it defaults to false and it is classified as dangerous even when enabled. Prefix folders are computed from a bounded sample (default 5 000 keys) split on a configurable separator, and folder counts are labelled as estimates.</p><p><strong>Why.</strong> <code>KEYS *</code> blocks the server for a full keyspace walk. On a production instance with ten million keys, a DB client that runs it on tree expand is an outage - and it is the single most common thing Redis GUIs get wrong. There is no correct exact answer available cheaply, so the choice is between a labelled estimate and a wrong number.</p><p><strong>Consequences.</strong> Folder counts are approximate and say so. Expanding a folder is a real scan and pages. A keyspace with no separators is one flat folder, which is the truth about that keyspace.</p><h2 id="adr-045-mongodb-s-schema-is-inferred-by-sampling-and-always-labelled-as-an-inference" tabindex="-1">ADR-045 - MongoDB&#39;s schema is inferred by sampling, and always labelled as an inference <a class="header-anchor" href="#adr-045-mongodb-s-schema-is-inferred-by-sampling-and-always-labelled-as-an-inference" aria-label="Permalink to &quot;ADR-045 - MongoDB&#39;s schema is inferred by sampling, and always labelled as an inference&quot;">​</a></h2><p><strong>Decided.</strong> The Fields section of the structure view, the completion index, and the grid&#39;s column union all come from sampling documents. Every surface that shows them says the sample size, and every field carries observed types with percentages and a presence percentage rather than a single type.</p><p><strong>Why.</strong> A collection has no schema. Showing &quot;the fields of this collection&quot; without qualification invites the reader to treat a 200-document sample as a guarantee, and the field that appears in 1% of documents is exactly the one that breaks their code. Percentages turn a false statement into a true one at no extra cost.</p><p><strong>Consequences.</strong> The Fields tab and the completion list can be wrong about a rare field, which is why they report presence. <code>$jsonSchema</code> validators, where a collection has one, are shown separately and are <em>not</em> an inference - the distinction is worth the two sections.</p><h2 id="adr-046-mongodb-and-redis-drivers-stay-pure-javascript" tabindex="-1">ADR-046 - MongoDB and Redis drivers stay pure JavaScript <a class="header-anchor" href="#adr-046-mongodb-and-redis-drivers-stay-pure-javascript" aria-label="Permalink to &quot;ADR-046 - MongoDB and Redis drivers stay pure JavaScript&quot;">​</a></h2><p><strong>Decided.</strong> <code>mongodb</code> (with <code>bson</code>&#39;s JS path) and <code>ioredis</code>. The optional native packages - <code>mongodb-client-encryption</code>, <code>kerberos</code>, <code>@mongodb-js/zstd</code>, <code>snappy</code> - are excluded from the bundle and the features that need them are not claimed.</p><p><strong>Why.</strong> ADR-012: a native module forces a per-platform VSIX and breaks in some remote and devcontainer images. That reasoning does not change because the engine did.</p><p><strong>Consequences.</strong> No client-side field-level encryption, no Kerberos, and no zstd/snappy wire compression for MongoDB; zlib compression is still available and is enough. If FLE is ever demanded, it is a separate optional extension, not a native dependency in the core VSIX.</p><h2 id="adr-047-engine-order-generalize-then-mongodb-then-redis-then-mysql" tabindex="-1">ADR-047 - Engine order: generalize, then MongoDB, then Redis, then MySQL <a class="header-anchor" href="#adr-047-engine-order-generalize-then-mongodb-then-redis-then-mysql" aria-label="Permalink to &quot;ADR-047 - Engine order: generalize, then MongoDB, then Redis, then MySQL&quot;">​</a></h2><p><strong>Decided.</strong> Phase 11 generalizes the contract, Phase 12 ships MongoDB, Phase 13 ships Redis, Phase 14 ships MySQL/MariaDB. This resolves OQ-4, which had MySQL second.</p><p><strong>Why the order changed.</strong> MySQL is the largest audience but it validates nothing: it is relational, it speaks SQL, and it would land against the existing contract almost unchanged - which is why it is also the cheapest to do <em>last</em>, and the best candidate to hand to a contributor once the driver-authoring guide exists.</p><p>MongoDB first among the non-relational engines because it exercises every part of the generalization at once - documents, missing fields, sampled schema, a non-SQL language, cursor paging, conditional transactions - while still being close enough to the existing model that the grid and the editor mostly hold. Redis second because it is the harshest test of the same contract: no schema, no rows, no rollback, no planner, and a keyspace that punishes a client for asking a naive question. If the contract survives Redis it will survive most things.</p><p><strong>Consequences.</strong> Roughly twelve weeks after v1.0 before MySQL exists, which is a real cost in reach. The judgement is that shipping two engines nobody else in this space handles well is worth more than being the fourth good Postgres+MySQL client.</p><h2 id="adr-048-cross-engine-querying-stays-out" tabindex="-1">ADR-048 - Cross-engine querying stays out <a class="header-anchor" href="#adr-048-cross-engine-querying-stays-out" aria-label="Permalink to &quot;ADR-048 - Cross-engine querying stays out&quot;">​</a></h2><p><strong>Decided.</strong> Reaffirmed, now that there is more than one shape of engine: no joins across connections, no federated query layer, no &quot;SQL over Mongo&quot;.</p><p><strong>Why.</strong> Every one of those is a query engine, not a client feature, and each is a product on its own. The non-goal was cheap to hold when every engine was relational; it needs restating now precisely because multi-model makes it sound plausible.</p><h2 id="adr-049-offset-paging-over-a-scan-is-a-written-down-skip-not-a-hidden-cost" tabindex="-1">ADR-049 - Offset paging over a SCAN is a written-down SKIP, not a hidden cost <a class="header-anchor" href="#adr-049-offset-paging-over-a-scan-is-a-written-down-skip-not-a-hidden-cost" aria-label="Permalink to &quot;ADR-049 - Offset paging over a SCAN is a written-down SKIP, not a hidden cost&quot;">​</a></h2><p><strong>Context.</strong> The data view asks a driver for &quot;rows 200 to 250 of this key&quot;. Two Redis containers can answer that directly - <code>LRANGE</code> and <code>ZRANGE</code> take index ranges. The rest cannot: <code>HSCAN</code>, <code>SSCAN</code> and <code>SCAN</code> are cursors, and a cursor cannot be resumed from a number. Page 5 of a hash means reading pages 1 to 4 and throwing them away.</p><p><strong>Decision.</strong> The driver writes that into the command: <code>HSCAN key 0 COUNT 50 SKIP 200</code>. <code>SKIP</code> is Pitta&#39;s, not Redis&#39; - it is stripped by the session, which then loops the cursor and discards what it was told to. The command bar shows it, so what the user sees is what the server was asked to do.</p><p><strong>Why not hide it.</strong> The alternative is a page control that looks like every other one in the product while quietly costing the server four extra passes over a hash. On a 100 000-field key that is a real amount of work done on a single-threaded server, and a client that does it invisibly is how a &quot;read-only browse&quot; turns into a production incident. The same reasoning as ADR-044: where Redis charges for a question, the UI says so.</p><p><strong>Consequences.</strong> Deep paging into a large hash is honest but not fast, and the grid&#39;s page control will eventually offer Next/Previous over a real cursor instead (<code>GridQuery.page.mode: &#39;cursor&#39;</code> exists for it). <code>SKIP</code> is also type-able in a <code>.redis</code> file and does the same thing there, so it is a feature of the client&#39;s dialect rather than a private protocol between two of its modules.</p><h2 id="adr-050-the-redis-driver-classifies-through-parsestatement-not-a-new-hook" tabindex="-1">ADR-050 - The Redis driver classifies through <code>parseStatement</code>, not a new hook <a class="header-anchor" href="#adr-050-the-redis-driver-classifies-through-parsestatement-not-a-new-hook" aria-label="Permalink to &quot;ADR-050 - The Redis driver classifies through `parseStatement`, not a new hook&quot;">​</a></h2><p><strong>Context.</strong> <code>10-ENGINE-MODEL.md</code> proposed <code>classifyCommand</code> as a new driver method for non-SQL guard classification.</p><p><strong>Decision.</strong> Redis implements the <em>existing</em> <code>parseStatement</code> hook instead. It returns the same <code>ParsedStatement</code> the Postgres parser does, and the host combines it with the shared heuristic under ADR-027&#39;s rule unchanged.</p><p><strong>Why.</strong> The hook already means &quot;the driver&#39;s own answer about what this statement is&quot;, and the combining rule it feeds is exactly the one wanted here. Adding a second method with the same purpose and a different name would have meant two guard paths to keep in step, which is how a safety mechanism develops a hole.</p><p><strong>Consequences.</strong> One less contract change than Phase 11 planned. The name reads oddly for an engine with no statements, and renaming it to <code>classifyCommand</code> across all drivers is worth doing when <code>Statement.sql</code> is renamed to <code>.text</code> (P11-T1) - one mechanical pass, not two.</p><h2 id="numbering-note" tabindex="-1">Numbering note <a class="header-anchor" href="#numbering-note" aria-label="Permalink to &quot;Numbering note&quot;">​</a></h2><p>ADR-034 through ADR-037 were written twice: the multi-model work and the UI search work were in flight at the same time and both numbered from 033. The four below are the search-and-streaming set, renumbered to 051-054; the multi-model set keeps 034-037 because the code and the engine documents cite those numbers throughout. Commit messages written before the renumber refer to the old numbers.</p><h2 id="adr-051-the-listening-session-is-its-own-connection" tabindex="-1">ADR-051 - The listening session is its own connection <a class="header-anchor" href="#adr-051-the-listening-session-is-its-own-connection" aria-label="Permalink to &quot;ADR-051 - The listening session is its own connection&quot;">​</a></h2><p><strong>Context.</strong> LISTEN holds a connection for as long as you want notifications, which is the point of it. The panel could borrow the session queries run on.</p><p><strong>Decision.</strong> It opens its own, through a new <code>connectDedicated</code> on the connection manager: same profile, same credentials from the keychain, same tunnel, but a session nothing else is ever handed. A session parked in LISTEN is a session the next query would queue behind, and the failure would look like &quot;Pitta got slow&quot; rather than &quot;the notify panel is holding the connection&quot;.</p><p>The session is still tracked, so disconnecting the profile closes it and the explorer still shows one connection. Closing the panel closes the session: leaving it open would park a backend on the server for the life of the window.</p><p>Channel discovery deserves a note. There is no catalog of channels - a channel exists only while something listens on it, and <code>pg_listening_channels()</code> reports only the current session. So the suggestion list is built from where notifications actually come <em>from</em>: <code>pg_notify(...)</code> and <code>NOTIFY ...</code> inside trigger and routine bodies, read out of <code>pg_proc.prosrc</code>.</p><h2 id="adr-052-streaming-is-pull-based-and-scrolling-is-the-backpressure" tabindex="-1">ADR-052 - Streaming is pull-based, and scrolling is the backpressure <a class="header-anchor" href="#adr-052-streaming-is-pull-based-and-scrolling-is-the-backpressure" aria-label="Permalink to &quot;ADR-052 - Streaming is pull-based, and scrolling is the backpressure&quot;">​</a></h2><p><strong>Context.</strong> P8-T3. Until now the runner read up to <code>maxRowsInMemory</code> before the first row could be drawn: a <code>SELECT *</code> over a large table paid the whole cost up front even though nobody was going to look past the first screen.</p><p><strong>Decision.</strong> The cursor stays open after its first page, and the panel pulls more when the reader scrolls near the end. There is no push, no queue, and no ack protocol - the webview asking <em>is</em> the backpressure signal, and a result nobody scrolls through is a result the server is never asked to produce.</p><p>Three constraints fall out of the session model and are enforced rather than documented:</p><ul><li>Only the last statement of a batch may keep a cursor open. A cursor holds the session, so statement N+1 would wait behind it.</li><li>A new run closes the previous run&#39;s cursors first, for the same reason.</li><li>Closing the panel closes them, or a backend sits idle-in-transaction with nobody left to read from it.</li></ul><p>Exhaustion is detected from a short batch rather than an empty one. A cursor returns fewer rows than asked for only at the end, so waiting for an empty fetch to prove it would add a round trip to every result.</p><p><strong>Consequences.</strong> <code>maxRowsInMemory</code> is now a ceiling on what a panel can accumulate by scrolling, not a cost paid on every query. The unit tests assert the thing that matters and is otherwise invisible - <em>how many times the server was read</em> - by counting fetches on a fake handle.</p><h2 id="adr-053-extension-types-get-a-view-not-a-decoded-value" tabindex="-1">ADR-053 - Extension types get a view, not a decoded value <a class="header-anchor" href="#adr-053-extension-types-get-a-view-not-a-decoded-value" aria-label="Permalink to &quot;ADR-053 - Extension types get a view, not a decoded value&quot;">​</a></h2><p><strong>Context.</strong> pgvector and PostGIS values are text and hex respectively, and neither is readable. The tempting fix is to decode them into something richer on the way through the host.</p><p><strong>Decision.</strong> They cross as text like everything else (ADR-004), and the <em>webview</em> interprets them for display only: a vector becomes a magnitude, a range, a zero count and a strip plot; a geometry becomes an SVG drawn from its own coordinates, with no basemap and no tile request, because the webview has no network access and should not have one (S5.1).</p><p>Nothing decoded is ever written back or used to build SQL. The raw text view is always present, which is what makes the interpretation safe to be approximate - a GeometryCollection is reported but not walked, and a truncated EWKB still yields a useful header.</p><h2 id="adr-054-search-is-one-box-rendered-by-the-engine-and-never-an-index-scan" tabindex="-1">ADR-054 - Search is one box, rendered by the engine, and never an index scan <a class="header-anchor" href="#adr-054-search-is-one-box-rendered-by-the-engine-and-never-an-index-scan" aria-label="Permalink to &quot;ADR-054 - Search is one box, rendered by the engine, and never an index scan&quot;">​</a></h2><p><strong>Context.</strong> UI mode had per-column filters with a full operator set, and no way to answer &quot;which rows mention this anywhere&quot;. Those are different questions: a filter is <em>this column is that</em>, search is <em>this text appears somewhere in the row</em>.</p><p><strong>Decision.</strong> <code>GridQuery</code> grows a <code>search</code> field, and the driver renders it - PostgreSQL as <code>col::text ILIKE $1</code> ORed across columns, SQLite as <code>CAST(col AS TEXT) LIKE ? ESCAPE &#39;\\&#39;</code>, because SQLite&#39;s LIKE is already case-insensitive for ASCII and has no default escape character. One bind parameter is referenced by every column rather than one per column, and the term is always bound, never interpolated (S3.1).</p><p>Three details are deliberate:</p><ul><li><strong>Wildcards are escaped.</strong> A user typing <code>50%</code> means fifty percent, not &quot;match every row&quot;, and that failure would look like the feature working.</li><li><strong>Binary columns are skipped.</strong> Casting a 40 MB bytea to text on every row to look for a word costs everything and finds nothing anyone wanted.</li><li><strong>Search is ANDed with the filters</strong>, so searching inside a filtered view narrows it rather than replacing it.</li></ul><p>Casting every column to text means no index can be used and the scan is sequential. That is inherent to the question, not a shortcoming to fix later, so the UI labels it <em>search</em> rather than presenting it as another filter, the input is debounced rather than firing per keystroke, and the tooltip says the scan cannot use an index.</p><p><strong>Consequences.</strong> A filter chip bar now shows what is applied - a filter row scrolled off to the right is easy to forget, and &quot;where did my rows go&quot; is a bad five minutes. Selecting a cell offers Filter and Exclude on its value, because &quot;show me the other rows like this one&quot; is the most common thing anyone wants from a cell they are already looking at, and NULL maps to IS NULL rather than to <code>= NULL</code>, which is never true.</p><h2 id="adr-055-the-text-a-driver-shows-and-the-operation-it-runs-are-built-together" tabindex="-1">ADR-055 - The text a driver shows and the operation it runs are built together <a class="header-anchor" href="#adr-055-the-text-a-driver-shows-and-the-operation-it-runs-are-built-together" aria-label="Permalink to &quot;ADR-055 - The text a driver shows and the operation it runs are built together&quot;">​</a></h2><p><strong>Context.</strong> The data view asks a driver for a read or an edit and gets back a <code>Statement</code>. For SQL that is text plus bound parameters, and the text is both what runs and what is shown above the grid. MongoDB has no such text: the driver speaks BSON, and the shell syntax people read is a <em>rendering</em> of it.</p><p>Rendering values into that text and parsing them back would undo the property S3.1 exists for - the values would have made a round trip through a string, and the escaping of that string would be the only thing standing between a value and the query.</p><p><strong>Decision.</strong> <code>buildSelect</code> and <code>buildEdits</code> return the rendered text in <code>sql</code> and the already-built operation in <code>params[0]</code>. The session runs the operation when it is given one and parses the text only when it is not - which is the case for a command someone typed themselves.</p><p><strong>Consequences.</strong> The command bar shows <code>db.orders.updateOne({_id: ObjectId(&quot;...&quot;)}, {$set: {...}})</code> and the server receives BSON that was never text. The cost is that two representations exist and could disagree; they are built in the same function from the same values, and the tests assert the pair.</p><p>The same shape is available to any driver whose engine does not take a string - which is most of the non-relational ones.</p><h2 id="adr-056-mysql-imports-through-batched-inserts-not-load-data" tabindex="-1">ADR-056 - MySQL imports through batched INSERTs, not LOAD DATA <a class="header-anchor" href="#adr-056-mysql-imports-through-batched-inserts-not-load-data" aria-label="Permalink to &quot;ADR-056 - MySQL imports through batched INSERTs, not LOAD DATA&quot;">​</a></h2><p><strong>Context.</strong> <code>bulkLoad</code> exists so an import is not a loop of single INSERTs (ADR-030). MySQL&#39;s fast path is <code>LOAD DATA LOCAL INFILE</code>.</p><p><strong>Decision.</strong> Use batched multi-row INSERTs inside one transaction instead.</p><p><strong>Why.</strong> <code>LOAD DATA LOCAL INFILE</code> needs <code>local_infile</code> enabled on the server <em>and</em> the client, and it has been off by default on both since MySQL 8.0 for good security reasons - a server can ask a client that allows it for any file the client can read. A fast path that fails on most servers, and that asks the user to turn off a protection to get it, is worse than a slower one that always works.</p><p><strong>Consequences.</strong> Import is a few times slower than <code>mysqlimport</code> and still orders of magnitude faster than one statement per row. If a user has <code>local_infile</code> on and wants it, that is a later opt-in with the trade written next to the setting.</p><h2 id="adr-057-a-selection-runs-exactly-what-is-selected" tabindex="-1">ADR-057 - A selection runs exactly what is selected <a class="header-anchor" href="#adr-057-a-selection-runs-exactly-what-is-selected" aria-label="Permalink to &quot;ADR-057 - A selection runs exactly what is selected&quot;">​</a></h2><p><strong>Context.</strong> Run honours a selection when there is one, and used to widen it out to whole statement boundaries: selecting the middle of a statement ran all of it, and selecting one of several ran from the first covered statement to the last.</p><p>That reads as helpful and is not. Highlighting is how someone says <em>this much and no more</em>. Two cases it got wrong:</p><ul><li>A file where nothing is terminated with a semicolon is one statement to the splitter, so selecting one query out of five ran all five.</li><li>Selecting one branch of a <code>WHERE</code> clause, or one arm of a <code>UNION</code>, ran the whole statement instead - which on DML is the expensive kind of surprise.</li></ul><p><strong>Decision.</strong> With a selection, Run executes the selected text verbatim. The text is still split, so selecting three statements runs three of them into three result tabs, and an empty selection says so rather than running something else.</p><p><strong>Consequences.</strong> Selecting half a statement produces a syntax error from the server, which is the correct answer to what was asked. The guards run on the selected text, so a destructive statement is still confirmed - what changed is which text is being judged, not whether it is.</p><h2 id="adr-058-sql-server-s-exact-numerics-are-converted-on-the-server" tabindex="-1">ADR-058 - SQL Server&#39;s exact numerics are converted on the server <a class="header-anchor" href="#adr-058-sql-server-s-exact-numerics-are-converted-on-the-server" aria-label="Permalink to &quot;ADR-058 - SQL Server&#39;s exact numerics are converted on the server&quot;">​</a></h2><p><strong>Context.</strong> ADR-004 says a <code>decimal</code> is a string and never a JS number. Every other driver satisfies it by asking its client library not to convert: <code>pg</code> returns numerics as text, <code>mysql2</code> does the same, MongoDB has <code>Decimal128</code>.</p><p>TDS does not offer the choice. <code>tedious</code> decodes <code>decimal</code>, <code>numeric</code> and <code>money</code> in its value parser and hands over a JavaScript number, so by the time any code in this repository sees the value, the digits past the fifteenth are gone. There is no option, no hook, and no pure-JS alternative client - the only driver that does better is <code>msnodesqlv8</code>, which is a native ODBC binding and is ruled out by ADR-012.</p><p><strong>Decision.</strong> Convert on the server for everything the grid reads: <code>buildSelect</code> projects an exact-numeric column as <code>CONVERT(varchar(41), [col]) AS [col]</code>. Values arrive as text, exact at any precision. For a statement the user typed, where there is nothing to rewrite, the number is rendered at the column&#39;s declared scale - exact for every precision a double holds, and no better.</p><p><strong>Why.</strong> The data view is where a decimal is read, compared and edited, and it is the path where being wrong would be silent: a total that renders as <code>12345678901234567000</code> looks like a number rather than like a failure. That path can be made exact, so it is. The ad-hoc path cannot be, and saying so is better than pretending.</p><p>Two details make the conversion safe. The alias keeps the column&#39;s own name, and the data view takes a column&#39;s <em>type</em> from the catalog rather than from the result, so the cell still edits as a decimal. And the generated SELECT aliases its table and qualifies every reference, because T-SQL resolves a bare <code>ORDER BY total</code> against the select list - which would have sorted the converted text instead of the number.</p><p><strong>Consequences.</strong> The conformance kit&#39;s exact-decimal case for this engine is written with the same <code>CONVERT</code>, which is what the driver itself does rather than a way around the check. A user who selects a <code>decimal(38,10)</code> column by hand and needs every digit has to ask for it the same way - and the driver&#39;s own statement shows them how.</p><h2 id="adr-059-every-sql-server-statement-is-wrapped-so-plans-need-a-pinned-batch" tabindex="-1">ADR-059 - Every SQL Server statement is wrapped, so plans need a pinned batch <a class="header-anchor" href="#adr-059-every-sql-server-statement-is-wrapped-so-plans-need-a-pinned-batch" aria-label="Permalink to &quot;ADR-059 - Every SQL Server statement is wrapped, so plans need a pinned batch&quot;">​</a></h2><p><strong>Context.</strong> <code>mssql</code> sends a parameterized statement through <code>sp_executesql</code>. That is what makes <code>@1</code> binding possible, and it has a consequence that reaches the rest of the driver: anything session-scoped applies to the <em>nested</em> batch and is gone when it returns. <code>USE [other_db]</code> changes nothing. <code>SET SHOWPLAN_ALL ON</code> - which must also be the only statement in its batch - plans the nested batch and returns nothing useful.</p><p><strong>Decision.</strong> Two rules. The introspection plan never says <code>USE</code>; it writes three-part names (<code>[db].sys.tables</code>), which is how one connection reads another database&#39;s catalog. And <code>explain</code> goes through <code>Session.runBatch</code>, which opens a transaction to pin one connection and sends <code>SET … ON</code>, the statement, and <code>SET … OFF</code> as three real batches on it.</p><p><strong>Why.</strong> The alternatives are worse in ways that are not obvious until they fail. Sending the three settings as separate pool requests looks correct and is not - each one may land on a different connection, so the setting and the statement end up on different sessions. Opening a second connection per database would multiply connections by the size of the tree.</p><p><strong>Consequences.</strong> <code>runBatch</code> takes no parameters, because <code>execSqlBatch</code> cannot carry them; its callers pass statement text that came from the user&#39;s own editor. An estimated plan rolls its transaction back, an analyzed one commits - <code>STATISTICS PROFILE</code> really runs the statement, which is the same promise <code>EXPLAIN ANALYZE</code> makes everywhere else. And <code>batch()</code> reports no column metadata, so those result sets are read as records keyed by column name rather than as arrays.</p><h2 id="adr-060-there-is-no-sql-there-are-four-engines-that-nearly-agree" tabindex="-1">ADR-060 - There is no &quot;SQL&quot;; there are four engines that nearly agree <a class="header-anchor" href="#adr-060-there-is-no-sql-there-are-four-engines-that-nearly-agree" aria-label="Permalink to &quot;ADR-060 - There is no &quot;SQL&quot;; there are four engines that nearly agree&quot;">​</a></h2><p><strong>Context.</strong> P11-T2 asked for the SQL statement splitter and formatter to move out of core and into <code>driver-pg</code>, so that core stopped applying one engine&#39;s grammar to every engine that said &quot;SQL&quot;. Taken literally that means four copies of a 240-line scanner and a 300-line printer, which is four places for one of them to drift.</p><p><strong>Decision.</strong> The scanner and the printer stay in core as shared code, and take a <code>SqlDialect</code>. Every SQL driver passes its own through <code>splitStatements</code> and <code>formatCommand</code>; core keeps <code>POSTGRES_DIALECT</code> only as the floor for a buffer that declares SQL and has no connection to ask - a <code>.sql</code> file bound to nothing.</p><p>Every flag on a dialect is off by default, so an engine that says nothing is scanned with ANSI rules and cannot inherit another engine&#39;s grammar by omission.</p><p><strong>Why.</strong> What was actually wrong was not the location of the code. It was that a MySQL buffer was being read with dollar quoting, nested block comments and psql&#39;s backslash commands, and without backticks or <code>#</code>; that a SQL Server buffer had <code>GO</code> handed to the server as if it were T-SQL, and <code>[odd;name]</code> split down the middle. Moving the file would not have fixed any of that, and parameterizing it fixes all of it. The rule the task was defending - core does not decide what an engine&#39;s statements look like - is satisfied by the driver supplying the dialect, not by the driver owning the loop.</p><p><strong>Consequences.</strong> Adding a SQL engine is a dialect constant and two one-line functions. A gap in the dialect vocabulary is a contract change, reviewed as one, rather than a fix buried in one driver where the other three keep the bug. The cost is that <code>@pitta/core</code> still exports a SQL scanner, which reads oddly in a multi-model client; it is the floor rather than the default, and the doc comment says so.</p>',241)])])}const g=t(s,[["render",r]]);export{p as __pageData,g as default};
