import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.CZo9GF0W.js";const k=JSON.parse('{"title":"12 — Redis Engine","description":"","frontmatter":{},"headers":[],"relativePath":"12-ENGINE-REDIS.md","filePath":"12-ENGINE-REDIS.md","lastUpdated":1789060268000}'),n={name:"12-ENGINE-REDIS.md"};function o(d,e,l,r,h,p){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="_12-—-redis-engine" tabindex="-1">12 — Redis Engine <a class="header-anchor" href="#_12-—-redis-engine" aria-label="Permalink to &quot;12 — Redis Engine&quot;">​</a></h1><p>Driver package <code>packages/driver-redis</code>, engine id <code>redis</code>, built on the pure-JS <code>ioredis</code> client (ADR-046). Phase 13, after MongoDB — the contract work is shared, and Redis is the engine that stresses it hardest.</p><p>Target: Redis 6.2 → 8.x and Valkey, standalone / sentinel / cluster, plus optional detection of the JSON and Time Series modules. Redis is not a database in the shape the rest of this product assumes, and most of the interesting decisions below come from that.</p><hr><h2 id="_1-what-redis-is-not" tabindex="-1">1. What Redis is not <a class="header-anchor" href="#_1-what-redis-is-not" aria-label="Permalink to &quot;1. What Redis is not&quot;">​</a></h2><ul><li><strong>No schema, no tables, no rows.</strong> A database is a flat keyspace. The tree&#39;s folder structure is something Pitta <em>invents</em> from key names (ADR-044).</li><li><strong>No cheap enumeration.</strong> <code>KEYS *</code> blocks the server for the duration of a full keyspace walk. On a production instance with ten million keys that is an outage. Every listing is <code>SCAN</code>-based, cursor-paged, bounded.</li><li><strong>No rollback.</strong> <code>MULTI</code>/<code>EXEC</code> is atomic in the sense that nothing else runs in between, but a command that fails at execution time does not undo the ones before it. <code>supportsAtomicEdits: false</code>, and the apply dialog says so in words (ADR-041).</li><li><strong>No server-side read-only session.</strong> Enforcement is client-side plus a recommendation to use an ACL user, and the UI says which it is.</li><li><strong>No query planner.</strong> <code>supportsExplain: false</code>. There is nothing to visualize and a fake plan tree would be worse than an absent tab.</li><li><strong>No count that is both exact and free.</strong> <code>DBSIZE</code> is exact for a database; the number of keys under a prefix is only ever an estimate from a sample.</li></ul><h2 id="_2-capabilities" tabindex="-1">2. Capabilities <a class="header-anchor" href="#_2-capabilities" aria-label="Permalink to &quot;2. Capabilities&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsCatalogs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // db0..dbN — but see cluster, below</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSchemas</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">schemaless</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">distinguishesMissing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // a field is present or the key is not there</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objectKinds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;catalog&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;keyspace&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;keyPrefix&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;key&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;stream&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;consumerGroup&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;serverNode&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;folder&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsCursors</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // SCAN is a cursor, but not a server-held one</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsCursorPaging</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsOffsetPaging</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // LRANGE is indexable; SCAN is not. See §6.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsServerSideSort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // ZSET reads come back ordered; nothing else does</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsServerSideFilter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // SCAN MATCH, and only glob patterns</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsMultiStatement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsNamedParameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsQueryCancel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // CLIENT UNPAUSE / CLIENT KILL of our own second connection</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsTransactions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // MULTI/EXEC</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsAtomicEdits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // ...without rollback</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSavepoints</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isolationLevels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsExplain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsNotifications</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // pub/sub and keyspace notifications</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsArrays</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsJson</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: &lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true when the JSON module is loaded</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsRowIdFallback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsDdlGeneration</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSchemaDiff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsReadOnlySessions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // client-side only, said out loud</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSessionMonitor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // CLIENT LIST / CLIENT KILL</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsHealthReport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // INFO, SLOWLOG, MEMORY</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsBulkLoad</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // pipelined</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsTtl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // first-class: TTL is a column and an editor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSecondaryIndexes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resultShapes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;rows&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;keyValue&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;message&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;none&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parameterStyle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;none&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">identifierCase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;exact&#39;</span></span></code></pre></div><h2 id="_3-connecting" tabindex="-1">3. Connecting <a class="header-anchor" href="#_3-connecting" aria-label="Permalink to &quot;3. Connecting&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Group</th><th>Fields</th></tr></thead><tbody><tr><td>general</td><td>name, group, color, environment, read-only</td></tr><tr><td>connection</td><td>mode (<code>standalone</code> / <code>sentinel</code> / <code>cluster</code>), host, port, database index (0–15, hidden in cluster mode), URI paste (<code>redis://</code>, <code>rediss://</code>)</td></tr><tr><td>sentinel</td><td>sentinel nodes (tags), master name, sentinel username/password (<code>SecretRef</code>)</td></tr><tr><td>cluster</td><td>seed nodes (tags), <code>enableReadyCheck</code>, replica reads (<code>READONLY</code>)</td></tr><tr><td>auth</td><td>username (ACL, Redis ≥ 6), password (<code>SecretRef</code>)</td></tr><tr><td>tls</td><td>enabled, CA, client cert, client key, key passphrase (<code>SecretRef</code>), servername (SNI), rejectUnauthorized</td></tr><tr><td>ssh</td><td>the existing <code>ssh2</code> tunnel, unchanged</td></tr><tr><td>advanced</td><td>connectTimeout, commandTimeout, keepAlive, client name (default <code>pitta/&lt;version&gt;</code>), scan count, key separator</td></tr></tbody></table><p>At connect Pitta runs <code>CLIENT SETNAME pitta/&lt;version&gt;</code>, so its own connections are identifiable in the monitor and killable without guesswork, and reads <code>COMMAND INFO</code>/<code>COMMAND DOCS</code>, <code>INFO server</code>, <code>INFO keyspace</code> and — when permitted — <code>CONFIG GET databases</code>.</p><p><strong><code>CONFIG</code> is often disabled</strong> on managed Redis. When it is, the database count comes from <code>INFO keyspace</code> (which only reports non-empty databases) and the tree says so rather than showing a wrong number of databases. Cluster mode has one logical database and the database level is replaced by a <strong>Nodes</strong> level.</p><h2 id="_4-the-tree" tabindex="-1">4. The tree <a class="header-anchor" href="#_4-the-tree" aria-label="Permalink to &quot;4. The tree&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Connection (env-colored)</span></span>
<span class="line"><span>├─ db0  (keys: 1,284,301)              ← DBSIZE, exact and free</span></span>
<span class="line"><span>│  ├─ session:                         ← prefix folder, from a bounded sample</span></span>
<span class="line"><span>│  │  ├─ session:abc123      [hash · ttl 3m 12s]</span></span>
<span class="line"><span>│  │  └─ … Load more</span></span>
<span class="line"><span>│  ├─ cache:user:            [12,4xx keys, estimated]</span></span>
<span class="line"><span>│  └─ (keys with no separator)</span></span>
<span class="line"><span>├─ db1 …</span></span>
<span class="line"><span>├─ Channels                            pub/sub channels seen (PUBSUB CHANNELS)</span></span>
<span class="line"><span>└─ Server</span></span>
<span class="line"><span>   ├─ Info                             INFO, by section</span></span>
<span class="line"><span>   ├─ Clients                          CLIENT LIST</span></span>
<span class="line"><span>   ├─ Slowlog                          SLOWLOG GET</span></span>
<span class="line"><span>   └─ Nodes / Replication              cluster or replication topology</span></span></code></pre></div><p>Prefix folders come from a <strong>bounded SCAN sample</strong> (default 5 000 keys, <code>pitta.redis.sampleKeys</code>), split on <code>pitta.redis.keySeparator</code> (default <code>:</code>). Counts shown on a folder are estimates and are labelled as estimates. Expanding a folder runs a real <code>SCAN MATCH prefix:*</code> and pages with the cursor. See ADR-044.</p><p><code>KEYS</code> is never issued. <code>pitta.redis.allowKeysCommand</code> exists, defaults false, and even when enabled is classified as a dangerous command and confirmed on production.</p><h2 id="_5-command-mode" tabindex="-1">5. Command mode <a class="header-anchor" href="#_5-command-mode" aria-label="Permalink to &quot;5. Command mode&quot;">​</a></h2><p><strong>Language id <code>redis</code>, files <code>.redis</code>.</strong> One command per line, the syntax people type into <code>redis-cli</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET session:abc123</span></span>
<span class="line"><span>HGETALL user:42</span></span>
<span class="line"><span>SCAN 0 MATCH cache:* COUNT 100</span></span>
<span class="line"><span>ZRANGE leaderboard 0 9 WITHSCORES</span></span></code></pre></div><p>Splitting is per line, with quoting rules matching <code>redis-cli</code> (double and single quotes, <code>\\xNN</code> escapes) so a value containing spaces survives. Comments start with <code>#</code>. A failure stops the batch (ADR-015).</p><p>Completion is command names with their arity and summary from <code>COMMAND DOCS</code> — the server&#39;s own documentation, so it matches the server actually connected to, modules included — plus key names from the sampled keyspace and field names for a hash under the cursor. Hover shows the command&#39;s summary, complexity, ACL categories and flags. This is the same principle as ADR-026: the engine&#39;s own metadata beats a bundled table that drifts.</p><p>Results render by reply type: a bulk string as <code>keyValue</code>, an array as <code>rows</code>, a map reply as field/value rows, <code>OK</code>/<code>PONG</code> as <code>message</code>.</p><h2 id="_6-browsing-a-key" tabindex="-1">6. Browsing a key <a class="header-anchor" href="#_6-browsing-a-key" aria-label="Permalink to &quot;6. Browsing a key&quot;">​</a></h2><p>Opening a key opens the data view. What the grid shows depends on the key&#39;s type, and <code>buildSelect</code> compiles the <code>GridQuery</code> into the right command:</p><table tabindex="0"><thead><tr><th>Type</th><th>Columns</th><th>Paging</th><th>Edits</th></tr></thead><tbody><tr><td>string</td><td>value</td><td>none (one value; detail pane for large ones)</td><td><code>SET</code></td></tr><tr><td>hash</td><td>field, value</td><td><code>HSCAN</code> cursor</td><td><code>HSET</code>, <code>HDEL</code></td></tr><tr><td>list</td><td>index, value</td><td><code>LRANGE</code> window — the one type with real offsets</td><td><code>LSET</code>, <code>LINSERT</code>, <code>LREM</code></td></tr><tr><td>set</td><td>member</td><td><code>SSCAN</code> cursor</td><td><code>SADD</code>, <code>SREM</code></td></tr><tr><td>zset</td><td>member, score</td><td><code>ZRANGE</code> window, ordered</td><td><code>ZADD</code>, <code>ZREM</code></td></tr><tr><td>stream</td><td>id, field, value</td><td><code>XRANGE</code> by id</td><td><code>XADD</code>, <code>XDEL</code> (+ consumer groups as children)</td></tr><tr><td>JSON (module)</td><td>path, value</td><td>JSON tree in the detail pane</td><td><code>JSON.SET</code>, <code>JSON.DEL</code></td></tr></tbody></table><p><strong>Paging a scan costs what it costs.</strong> <code>LRANGE</code> and <code>ZRANGE</code> take index ranges, so paging them is exact. The other containers are cursors, and a cursor cannot be resumed from a number: page 5 of a hash means reading pages 1 to 4 and discarding them. That is written into the command as a <code>SKIP</code> suffix the client strips and honours, so the command bar shows what the server was actually asked to do rather than a page number that looks free (ADR-049).</p><p>Above every key view: its type, encoding (<code>OBJECT ENCODING</code>), memory usage (<code>MEMORY USAGE</code>, sampled), and its <strong>TTL as an editable field</strong> — <code>EXPIRE</code>, <code>PEXPIREAT</code> and <code>PERSIST</code> are one-click, guarded on production like any other write. TTL is not an afterthought here; it is often the most important thing about a key.</p><p>Scores are decimals and stay strings on the wire (ADR-004). Stream ids stay strings. Binary-safe values that are not valid UTF-8 render as hex with the detail pane&#39;s hex view, and are never mangled into replacement characters.</p><p><code>buildEdits</code> output is previewed exactly as generated SQL is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>HSET user:42 email &quot;new@example.com&quot;</span></span>
<span class="line"><span>HDEL user:42 legacy_flag</span></span>
<span class="line"><span>EXPIRE user:42 3600</span></span></code></pre></div><p>Applied inside <code>MULTI</code>/<code>EXEC</code> when the keys allow it, with the dialog stating that there is no rollback (ADR-041). In cluster mode a batch touching more than one slot cannot be a single transaction, and the dialog says that instead.</p><h2 id="_7-pub-sub" tabindex="-1">7. Pub/Sub <a class="header-anchor" href="#_7-pub-sub" aria-label="Permalink to &quot;7. Pub/Sub&quot;">​</a></h2><p>The LISTEN/NOTIFY subscriber panel (P6-T6) generalizes: a driver with <code>supportsNotifications</code> supplies <code>Session.listen(channel)</code>. Redis implements it over <code>SUBSCRIBE</code> / <code>PSUBSCRIBE</code> on a dedicated connection, and adds <strong>keyspace notifications</strong> (<code>__keyevent@0__:*</code>) as a preset — watching keys expire or be evicted live is one of the few genuinely diagnostic things a Redis client can offer. Enabling <code>notify-keyspace-events</code> requires <code>CONFIG SET</code>; Pitta shows the required setting and does not change it silently.</p><p><code>MONITOR</code> is deliberately <strong>not</strong> wired to a panel by default. It streams every command the server executes and measurably degrades it. It is available behind <code>pitta.redis.allowMonitor</code>, off by default, time-boxed to 60 seconds with a visible countdown, and refused outright on a <code>prod</code> profile.</p><h2 id="_8-monitoring-and-health" tabindex="-1">8. Monitoring and health <a class="header-anchor" href="#_8-monitoring-and-health" aria-label="Permalink to &quot;8. Monitoring and health&quot;">​</a></h2><p><code>readActivity</code> from <code>CLIENT LIST</code>: id, address, name, age, idle, current command, database, subscription counts, memory used. <code>stopBackend</code> maps to <code>CLIENT KILL ID</code>, and Pitta refuses to kill its own connections without an extra confirmation.</p><p><code>readHealth</code> findings from <code>INFO</code>, <code>SLOWLOG</code>, <code>MEMORY STATS</code> and a sampled key walk:</p><ul><li><strong>Memory</strong>: <code>used_memory</code> against <code>maxmemory</code>, fragmentation ratio, eviction policy, <code>evicted_keys</code> rate. An instance with <code>maxmemory: 0</code> and no eviction policy is a finding on its own — it will be OOM-killed rather than evict.</li><li><strong>Persistence</strong>: <code>rdb_last_bgsave_status</code>, <code>aof_last_write_status</code>, time since last successful save, AOF rewrite in progress.</li><li><strong>Replication</strong>: link status, offset lag per replica, <code>master_link_down_since</code>.</li><li><strong>Latency</strong>: <code>SLOWLOG</code> entries with their commands and durations, blocked clients, <code>latencystats</code> percentiles where available.</li><li><strong>Keyspace hygiene</strong> (sampled, opt-in, states its sample size): the proportion of keys with no TTL in a cache-shaped workload, and the largest keys found by <code>MEMORY USAGE</code> over the sample — the &quot;one 400 MB hash&quot; problem that nothing else in the tree would show.</li></ul><p>Nothing polls by default (ADR-025).</p><h2 id="_9-safety" tabindex="-1">9. Safety <a class="header-anchor" href="#_9-safety" aria-label="Permalink to &quot;9. Safety&quot;">​</a></h2><p>Redis is the engine where the guards earn their place, because the destructive commands are short, unqualified, and instantaneous.</p><ul><li><strong>Classification comes from the server.</strong> <code>COMMAND INFO</code> flags every command <code>readonly</code>, <code>write</code>, <code>admin</code>, <code>dangerous</code>, <code>blocking</code>, <code>noscript</code>, <code>denyoom</code>. Pitta reads that table at connect and classifies from it, so a module command the client has never heard of is still classified correctly (ADR-040).</li><li><strong>Always destructive</strong>: <code>FLUSHALL</code>, <code>FLUSHDB</code>, <code>SWAPDB</code>, <code>SHUTDOWN</code>, <code>CONFIG SET</code>, <code>CONFIG RESETSTAT</code>, <code>DEBUG</code>, <code>SCRIPT FLUSH</code>, <code>FUNCTION FLUSH</code>, <code>MIGRATE</code>, <code>REPLICAOF</code>, <code>SLAVEOF</code>, <code>FAILOVER</code>, <code>CLUSTER RESET</code>, <code>CLIENT KILL</code>, <code>ACL SETUSER</code>/<code>DELUSER</code>. Plus <code>RENAME</code> onto an existing key, and <code>DEL</code>/<code>UNLINK</code> with more than one key.</li><li><strong>Always refused, not merely confirmed</strong>: <code>KEYS</code> (unless the setting is on), and <code>MONITOR</code> on production.</li><li><strong><code>EVAL</code> / <code>EVALSHA</code> / <code>FCALL</code></strong> run arbitrary Lua. Classified as a write and an admin operation unless the script declares itself <code>no-writes</code>, and always confirmed on production.</li><li><strong>Unknown commands fail closed on production</strong> — the rule from §10 of <code>10-ENGINE-MODEL.md</code>, and Redis is why it exists.</li><li><strong>Read-only profiles</strong> refuse anything the flag table calls <code>write</code> or <code>admin</code>, and the badge reads &quot;client-side only&quot;. The connection form recommends an ACL user restricted with <code>+@read</code>, which is the only real enforcement.</li><li><strong>Values are sent as arguments, never spliced into a command string.</strong> <code>ioredis</code> takes argv arrays; there is no formatting step for user input to escape from. That is S3.1 for this engine, and it is structurally stronger than it is for SQL.</li></ul><h2 id="_10-testing" tabindex="-1">10. Testing <a class="header-anchor" href="#_10-testing" aria-label="Permalink to &quot;10. Testing&quot;">​</a></h2><ul><li><code>docker-compose.test.yml</code> gains Redis 6.2, 7.x and 8.x, plus one <code>redis-stack</code> service for the JSON module and one three-node cluster.</li><li><code>test/fixtures/redis-seed.txt</code>: keys of every type; a key with binary non-UTF-8 content; a key with a 5 MB value; a hash with 100 000 fields for <code>HSCAN</code> paging; a zset with float scores that must not lose precision; keys with separators at several depths, keys with no separator, keys with unicode and spaces; TTLs at second and millisecond precision; a stream with consumer groups and pending entries; 1M keys for the tree benchmark.</li><li>Conformance kit <code>EngineFixture</code> for Redis. The assertions Redis specifically proves: that a <code>supportsAtomicEdits: false</code> engine reports exactly which edits were applied when a batch fails; that no code path issues <code>KEYS</code>; that a <code>prod</code> profile refuses an unknown command; that the tree never loads more than <code>treePageSize</code> keys per request regardless of keyspace size.</li></ul><hr><h2 id="_11-what-is-built" tabindex="-1">11. What is built <a class="header-anchor" href="#_11-what-is-built" aria-label="Permalink to &quot;11. What is built&quot;">​</a></h2><p>The driver package is <code>packages/driver-redis</code>, registered alongside PostgreSQL and SQLite, and it needed two changes to core rather than none: imperative paged introspection (ADR-037), and the honest non-atomic apply path (ADR-041). Both were Phase 11 items that Redis is the reason for.</p><p><strong>Working.</strong> Connecting (standalone, sentinel, cluster; ACL auth; TLS), pasting a <code>redis://</code> or <code>rediss://</code> URI to fill the form, the keyspace tree with SCAN-derived prefix folders, browsing and editing every container type through the ordinary data view, the structure view for a key, the client monitor, the health report, and the guards - refusals, the client-side read-only enforcement, server-flag classification, and unknown failing closed on production. Unit-tested against a scripted server, with an integration suite that seeds a real one and skips when none is reachable.</p><p><strong>Not built yet.</strong> The <code>.redis</code> editor language and its runner, so commands are issued by panels rather than typed into a file. The pub/sub panel, though <code>Session.listen</code> is implemented under it. The TTL editor action, though the command builder and the display exist. The settings that would relax <code>KEYS</code> and <code>MONITOR</code>: both are refused unconditionally today, which is the safe end of that decision but not the documented one. The SSH tunnel. The cluster node level. The sampled big-key report.</p><p>Task-level status is in <code>07-TASKS.md</code> under Phase 13.</p>`,50)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
