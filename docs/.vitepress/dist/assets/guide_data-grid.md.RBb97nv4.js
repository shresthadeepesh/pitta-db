import{_ as t,o as a,c as o,a2 as i}from"./chunks/framework.CZo9GF0W.js";const p=JSON.parse('{"title":"The data grid","description":"","frontmatter":{},"headers":[],"relativePath":"guide/data-grid.md","filePath":"guide/data-grid.md","lastUpdated":1789060268000}'),n={name:"guide/data-grid.md"};function r(s,e,d,l,c,h){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="the-data-grid" tabindex="-1">The data grid <a class="header-anchor" href="#the-data-grid" aria-label="Permalink to &quot;The data grid&quot;">​</a></h1><p>Click a table in the tree, or run <strong>Pitta: Open Data</strong>, and the grid opens. On MongoDB it opens on a collection; on Redis it opens on a key, in the view its type deserves.</p><h2 id="reading" tabindex="-1">Reading <a class="header-anchor" href="#reading" aria-label="Permalink to &quot;Reading&quot;">​</a></h2><p><strong>Paging.</strong> Rows come from a server-side cursor where the engine has one, in pages of <code>pitta.defaultFetchSize</code> (500 by default), fetched as you scroll. A million-row table opens as fast as a small one because Pitta never asked for a million rows. Where an engine has no cursor, the generated statement falls back to a limit and an offset, and the grid says which it is using.</p><p><strong>The SQL is always visible.</strong> Whatever Pitta ran to fill the grid — including the sort, the filter, and the key it identified rows by — is shown. Nothing is hidden behind the view.</p><p><strong>Sort and filter run on the server.</strong> They apply to the whole table, not to the page you happen to have. Click a header to sort; use the filter row for per-column operators (<code>=</code>, <code>&lt;&gt;</code>, <code>&gt;</code>, <code>like</code>, <code>in</code>, <code>is null</code>, and — since the contract stopped assuming SQL — <code>exists</code>, <code>not exists</code> and <code>regex</code> where the engine supports them).</p><p><strong>Columns</strong> can be hidden, pinned, reordered and resized, and the arrangement is remembered per table.</p><h2 id="values-stay-exact" tabindex="-1">Values stay exact <a class="header-anchor" href="#values-stay-exact" aria-label="Permalink to &quot;Values stay exact&quot;">​</a></h2><ul><li><code>numeric</code> and <code>decimal</code> are strings, end to end. They are displayed exactly, compared as strings, and edited as strings. They never pass through a JavaScript number, because that is where the trailing digits go.</li><li><code>int8</code> / <code>bigint</code> is a <code>bigint</code> or a string. Never silently truncated.</li><li><code>NULL</code> is a styled token, distinct from an empty string and from zero, and <code>IS NULL</code> is a separate filter operator rather than a value you type.</li><li><strong>A missing field is not null.</strong> On a schemaless engine the grid shows a distinct token for a field this document simply does not have, and clearing a cell asks which you meant: set it to null, or remove the field.</li><li><code>timestamptz</code> carries the session time zone, shows its offset, and can be toggled to UTC.</li><li>An unknown type is displayed as its raw text rather than throwing. Something is always shown.</li></ul><h2 id="editing" tabindex="-1">Editing <a class="header-anchor" href="#editing" aria-label="Permalink to &quot;Editing&quot;">​</a></h2><p>Double-click a cell to edit it. Each type gets a real editor rather than a text box: a tri-state boolean, a date/time picker that understands the column&#39;s time zone, an enum dropdown, a JSON editor that validates, array chips, a foreign-key picker that looks up the referenced row, and upload/download for binary columns.</p><p>Edits are <strong>staged</strong>. Nothing is sent while you type. When you apply:</p><ol><li>You get the exact statements Pitta will run, including which key identifies each row.</li><li>They are applied in <strong>one transaction</strong> where the engine has one.</li><li>Affected row counts are verified. A statement that matched a different number of rows than expected rolls the whole thing back.</li></ol><p>Where an engine cannot apply a batch atomically — a standalone MongoDB, a Redis command list crossing cluster slots — the dialog says so in those words, and the result reports which edits were applied and which were not. A rollback promise is never carried across from an engine that has one to an engine that does not.</p><p><strong>Insert</strong>, <strong>duplicate</strong> and <strong>delete row</strong> work the same way: staged, previewed, applied together.</p><h3 id="identifying-a-row" tabindex="-1">Identifying a row <a class="header-anchor" href="#identifying-a-row" aria-label="Permalink to &quot;Identifying a row&quot;">​</a></h3><p>Editing needs to name the row being changed:</p><ol><li>A primary key, if there is one.</li><li>A unique constraint, if there is not.</li><li>A physical row id — <code>ctid</code> on PostgreSQL, <code>rowid</code> on SQLite — as an opt-in fallback, with the caveat stated: it is stable only until the row moves.</li></ol><p>If none of those exists — a view, a table with no key and no row-id support — editing is disabled and the grid tells you <em>why</em>, rather than presenting a control that fails on apply.</p><h2 id="structure" tabindex="-1">Structure <a class="header-anchor" href="#structure" aria-label="Permalink to &quot;Structure&quot;">​</a></h2><p>The <strong>Structure</strong> tab is the object as the catalog describes it: columns, indexes, constraints, triggers, policies, partitions and statistics on a relational table; inferred fields, indexes, validation rules, sharding and stats on a MongoDB collection.</p><p><strong>Show DDL</strong> generates the object&#39;s definition. On engines whose catalog carries the source text it is the server&#39;s own; where it does not, Pitta composes a <code>CREATE TABLE</code> from the catalog rather than guessing.</p><h2 id="getting-data-out" tabindex="-1">Getting data out <a class="header-anchor" href="#getting-data-out" aria-label="Permalink to &quot;Getting data out&quot;">​</a></h2><p><strong>Copy</strong> the selection as TSV, CSV, JSON, Markdown, or SQL <code>INSERT</code> statements.</p><p><strong>Export</strong> the whole result — CSV, JSON, JSONL, XLSX, or SQL inserts — streamed to disk with progress and a cancel button. The result is never fully materialized in memory, so exporting a table larger than RAM is an ordinary thing to do.</p><p>On MongoDB, <strong>Pitta: Export Documents…</strong> writes Extended JSON, so an <code>ObjectId</code> comes back as an <code>ObjectId</code> rather than as <code>{}</code>.</p><h2 id="getting-data-in" tabindex="-1">Getting data in <a class="header-anchor" href="#getting-data-in" aria-label="Permalink to &quot;Getting data in&quot;">​</a></h2><p><strong>Pitta: Import CSV…</strong> on a table maps columns, previews the type coercion for every column, and reports what would fail before anything is written. Where the engine has a bulk path — PostgreSQL <code>COPY</code>, MySQL <code>LOAD DATA LOCAL INFILE</code> — it is used.</p><p><strong>Pitta: Import Documents…</strong> on a MongoDB collection reads a JSON array or NDJSON; the format is detected from the file rather than asked for.</p><p>Imported values obey the same exactness rules as everything else: a <code>numeric</code> column is not routed through a float on the way in.</p><h2 id="redis" tabindex="-1">Redis <a class="header-anchor" href="#redis" aria-label="Permalink to &quot;Redis&quot;">​</a></h2><p>A Redis key opens in the view its type deserves — string, hash, list, set, sorted set, stream with its consumer groups, or a JSON document — and <strong>TTL is a first-class, editable field</strong> rather than something to remember a command for. <strong>Pitta: Set Expiry…</strong> sets or clears it; clearing is <code>PERSIST</code>, not an expiry of zero.</p><p>The keyspace tree is built with <code>SCAN</code> in bounded pages. <code>KEYS</code> is refused by default (<code>pitta.redis.allowKeysCommand</code>) because it walks the whole keyspace and blocks the server while it does.</p>',33)])])}const u=t(n,[["render",r]]);export{p as __pageData,u as default};
