import{_ as s,o as i,c as t,a2 as a}from"./chunks/framework.CZo9GF0W.js";const k=JSON.parse('{"title":"11 — MongoDB Engine","description":"","frontmatter":{},"headers":[],"relativePath":"11-ENGINE-MONGODB.md","filePath":"11-ENGINE-MONGODB.md","lastUpdated":1789060268000}'),n={name:"11-ENGINE-MONGODB.md"};function o(d,e,l,r,h,p){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="_11-—-mongodb-engine" tabindex="-1">11 — MongoDB Engine <a class="header-anchor" href="#_11-—-mongodb-engine" aria-label="Permalink to &quot;11 — MongoDB Engine&quot;">​</a></h1><p>Driver package <code>packages/driver-mongodb</code>, engine id <code>mongodb</code>, built on the pure-JS <code>mongodb</code> Node driver (ADR-046). Phase 12. Depends on the contract changes in <code>10-ENGINE-MODEL.md</code> being landed first.</p><p>Target: MongoDB 6.0 → 8.x, self-hosted or Atlas, standalone / replica set / sharded. DocumentDB and Cosmos&#39; Mongo API are <em>not</em> claimed — they answer to a subset of the same wire protocol, and claiming them without CI against them would be an aspirational driver.</p><hr><h2 id="_1-capabilities" tabindex="-1">1. Capabilities <a class="header-anchor" href="#_1-capabilities" aria-label="Permalink to &quot;1. Capabilities&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsCatalogs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // databases</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSchemas</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // there is no level between database and collection</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">schemaless</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">distinguishesMissing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">objectKinds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;catalog&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;folder&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;collection&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;view&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;index&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;field&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;serverNode&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsCursors</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           // a real server cursor, batched</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsMultiStatement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // one shell command per line-group</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsNamedParameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsQueryCancel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // killOp, found by comment — ADR-043</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsTransactionalDdl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsTransactions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: &lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// replica set or sharded only — ADR-035</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsAtomicEdits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: &lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSavepoints</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isolationLevels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;read uncommitted&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;snapshot&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> available</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsExplain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsExplainAnalyze</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // executionStats actually runs the query</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsNotifications</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // change streams, on a replica set</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsArrays</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsJson</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsRowIdFallback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // _id always exists; there is no fallback to need</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsDdlGeneration</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // createCollection + createIndexes script</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSchemaDiff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // [P2] index/validator diff only</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsReadOnlySessions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // enforced client-side + a read-only user</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSessionMonitor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // $currentOp</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsHealthReport</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsBulkLoad</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // insertMany, ordered:false, batched</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsCursorPaging</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsOffsetPaging</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // skip/limit, with the usual deep-page warning</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsServerSideSort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsServerSideFilter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsAggregation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsTtl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // TTL indexes</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportsSecondaryIndexes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resultShapes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;documents&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;message&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;none&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parameterStyle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;none&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">identifierCase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;exact&#39;</span></span></code></pre></div><h2 id="_2-connecting" tabindex="-1">2. Connecting <a class="header-anchor" href="#_2-connecting" aria-label="Permalink to &quot;2. Connecting&quot;">​</a></h2><p>Rendered from <code>connectionSchema</code> like every other engine — no bespoke form.</p><table tabindex="0"><thead><tr><th>Group</th><th>Fields</th></tr></thead><tbody><tr><td>general</td><td>name, group, color, environment, read-only</td></tr><tr><td>connection</td><td>scheme (<code>mongodb</code> / <code>mongodb+srv</code>), hosts (tags field, <code>host:port</code> each), database, authSource, replicaSet, directConnection, readPreference, appName (default <code>pitta/&lt;version&gt;</code>)</td></tr><tr><td>auth</td><td>mechanism (<code>SCRAM-SHA-256</code>, <code>SCRAM-SHA-1</code>, <code>MONGODB-X509</code>, <code>MONGODB-AWS</code>, <code>DEFAULT</code>), username, password (<code>SecretRef</code>)</td></tr><tr><td>tls</td><td>enabled, CA file, client cert, client key, key passphrase (<code>SecretRef</code>), allowInvalidCertificates, allowInvalidHostnames, tlsInsecure</td></tr><tr><td>ssh</td><td>the existing <code>ssh2</code> tunnel, unchanged</td></tr><tr><td>advanced</td><td>connectTimeoutMS, socketTimeoutMS, serverSelectionTimeoutMS, maxPoolSize, <code>maxTimeMS</code> default, compressors, retryWrites, retryReads</td></tr></tbody></table><p><code>parseConnectionUri</code> accepts a full <code>mongodb://</code> / <code>mongodb+srv://</code> string, strips the password straight into SecretStorage and never echoes it (A1, S1.3). <code>mongodb+srv</code> implies TLS; the form says so rather than letting the user think they turned it off.</p><p>S2.1 applies unchanged: any non-loopback host defaults to TLS with certificate validation, and <code>allowInvalidCertificates</code> or <code>tlsInsecure</code> badges the connection &quot;insecure TLS&quot; in the tree.</p><p><strong>Topology probe at connect.</strong> <code>hello()</code> tells us whether this is a replica set or a sharded cluster, which decides <code>supportsTransactions</code> and <code>supportsAtomicEdits</code> for the session (ADR-035), and whether change streams are available for the notifications panel.</p><h2 id="_3-the-tree" tabindex="-1">3. The tree <a class="header-anchor" href="#_3-the-tree" aria-label="Permalink to &quot;3. The tree&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Connection (env-colored)</span></span>
<span class="line"><span>└─ Databases                       (admin/local/config collapsed unless showSystemObjects)</span></span>
<span class="line"><span>   └─ Collections                  (+ document count estimate, storage size)</span></span>
<span class="line"><span>      ├─ Indexes                   (keys, unique, sparse, partial, TTL, hidden)</span></span>
<span class="line"><span>      └─ Fields          [inferred] sampled key/type profile — ADR-045</span></span>
<span class="line"><span>   ├─ Views                        (name + the pipeline that defines it)</span></span>
<span class="line"><span>   └─ (Atlas) Search indexes       [P2]</span></span>
<span class="line"><span>└─ Server                          hello(), build info, topology, members</span></span></code></pre></div><p>Collection counts use <code>estimatedDocumentCount()</code> — the metadata count, free — which is exactly what <code>estimateRowCount</code> was added for in ADR-021. Exact <code>countDocuments()</code> stays on demand, and says which one is being shown.</p><p>A database with thousands of collections pages through the <code>Load more</code> node (§3 of <code>10-ENGINE-MODEL.md</code>).</p><h2 id="_4-query-mode" tabindex="-1">4. Query mode <a class="header-anchor" href="#_4-query-mode" aria-label="Permalink to &quot;4. Query mode&quot;">​</a></h2><p><strong>Language id <code>mongodb</code>, files <code>.mongodb.js</code>.</strong> The dialect is the mongosh subset people already write:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db.orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">find</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, { _id: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, total: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ total: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">limit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db.orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aggregate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([{ $match: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }, { $group: { _id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;$customerId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, n: { $sum: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } }])</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db.orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">updateMany</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;draft&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, { $set: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">runCommand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ collStats: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orders&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span></code></pre></div><p><strong>It is parsed, never evaluated (ADR-042).</strong> The text goes through <code>acorn</code> to an AST; the AST is matched against an allowlist of call shapes; object and array literals are converted to BSON directly from the syntax tree, with the constructor forms mongosh uses (<code>ObjectId</code>, <code>ISODate</code>, <code>NumberLong</code>, <code>NumberDecimal</code>, <code>UUID</code>, <code>BinData</code>, <code>/regex/flags</code>) recognised as literals. No <code>eval</code>, no <code>vm</code>, no <code>new Function</code>. Anything outside the allowlist — a variable, a function call we do not know, control flow — is a diagnostic, not a fallback to execution.</p><p>Consequences, stated up front: no loops, no <code>let</code>, no user-defined helpers in a <code>.mongodb.js</code> file. That is a real limitation against mongosh and the right trade for a client that executes what someone pastes from a ticket.</p><ul><li><strong>Splitting.</strong> Statements split on top-level statement boundaries in the AST, so a multi-line pipeline is one unit and a file of ten commands is ten result tabs. Same execution semantics as SQL: a failure stops the batch (ADR-015).</li><li><strong>Intelligence.</strong> Completion of database names, collection names, and <em>fields from the sampled schema</em>, plus operator keywords (<code>$match</code>, <code>$lookup</code>, …) with signatures. Hover on a field shows the sampled types and presence. Backed by the same metadata cache; the sample is what <code>readSymbols</code> returns for this engine.</li><li><strong>Diagnostics.</strong> Unparseable text, disallowed constructs, unknown collection (warning, suppressed when the index is empty — ADR-027&#39;s caution applies unchanged), unknown top-level operator.</li><li><strong>Cancel.</strong> Real, see ADR-043.</li></ul><h2 id="_5-the-grid" tabindex="-1">5. The grid <a class="header-anchor" href="#_5-the-grid" aria-label="Permalink to &quot;5. The grid&quot;">​</a></h2><p>A result is <code>documents</code>. The column union is sampled from the fetched page (default 200 docs), <code>_id</code> first, then by descending frequency, then alphabetically for ties. Nested objects are flattened one level into dotted paths when the dotted name does not collide with a real top-level field; otherwise the object stays a single cell. Arrays render as an <code>[n]</code> chip.</p><p>Every cell keeps its raw BSON in the detail pane, which offers JSON tree / extended-JSON text / hex-for-binary, with raw text always last (ADR-033).</p><p>Type mapping, with ADR-004 intact:</p><table tabindex="0"><thead><tr><th>BSON</th><th>ValueKind</th><th>Notes</th></tr></thead><tbody><tr><td>Double</td><td>number</td><td></td></tr><tr><td>Decimal128</td><td>decimal</td><td><strong>string on the wire, always</strong></td></tr><tr><td>Int32</td><td>number</td><td></td></tr><tr><td>Int64</td><td>bigint</td><td>never through <code>Number</code></td></tr><tr><td>String</td><td>string</td><td></td></tr><tr><td>ObjectId</td><td>uuid-like</td><td>rendered as the 24-hex form, copyable</td></tr><tr><td>Date</td><td>timestamp</td><td>UTC; the grid&#39;s timezone toggle applies</td></tr><tr><td>Timestamp</td><td>timestamp</td><td>the internal oplog type, labelled as such</td></tr><tr><td>Boolean</td><td>bool</td><td></td></tr><tr><td>Null</td><td>—</td><td>renders <code>NULL</code></td></tr><tr><td><em>absent</em></td><td>—</td><td>renders <code>MISSING</code> (ADR-039)</td></tr><tr><td>Object / Array</td><td>json / array</td><td>tree in the detail pane</td></tr><tr><td>Binary</td><td>binary</td><td>subtype shown; images detected by magic bytes (ADR-033)</td></tr><tr><td>Regex</td><td>string</td><td><code>/pattern/flags</code></td></tr><tr><td>MinKey / MaxKey</td><td>unknown</td><td>rendered as their literal token</td></tr></tbody></table><h2 id="_6-editing" tabindex="-1">6. Editing <a class="header-anchor" href="#_6-editing" aria-label="Permalink to &quot;6. Editing&quot;">​</a></h2><p>A document result is editable when every document in it came from one collection and carries <code>_id</code>. An aggregation result is read-only with the reason shown, unless the pipeline is a pure <code>$match</code>/<code>$sort</code>/<code>$project</code>/<code>$limit</code> chain over one collection, in which case identity survives and editing is allowed.</p><p>Generated commands, always previewed before they run:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db.orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">updateOne</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ _id: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ObjectId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) }, { $set: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, $unset: { legacyFlag: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db.orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">insertOne</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ … })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db.orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">deleteOne</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ _id: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ObjectId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) })</span></span></code></pre></div><ul><li>Clearing a field to <code>null</code> and removing a field are <strong>two different actions</strong> producing <code>$set: null</code> and <code>$unset</code> (ADR-039).</li><li>Editing a field inside a nested object or an array element uses a dotted or positional path; the preview shows the exact path so an accidental <code>orders.0.total</code> is visible before it runs.</li><li>Apply runs in a transaction when the session reports one is available, and says plainly that it cannot when it is not (ADR-041).</li><li>S4.5&#39;s affected-count check maps to <code>matchedCount</code>/<code>modifiedCount</code>; a <code>matchedCount</code> of 0 on an update aborts the batch — that is the stale-key mass-update guard, and it works here too.</li></ul><h2 id="_7-structure-view" tabindex="-1">7. Structure view <a class="header-anchor" href="#_7-structure-view" aria-label="Permalink to &quot;7. Structure view&quot;">​</a></h2><p><code>readStructure</code> returns, as <code>StructureSection[]</code> (ADR-032, unchanged):</p><table tabindex="0"><thead><tr><th>Section</th><th>Contents</th></tr></thead><tbody><tr><td>fields</td><td><strong>Inferred.</strong> name, observed types with percentages, presence %, sample size, and a note saying it is a sample (ADR-045)</td></tr><tr><td>indexes</td><td>name, key spec, unique, sparse, partial filter, TTL seconds, hidden, size, usage count from <code>$indexStats</code></td></tr><tr><td>validation</td><td>the <code>$jsonSchema</code> validator, validationLevel, validationAction</td></tr><tr><td>sharding</td><td>shard key, chunk counts per shard, balancer state — omitted when not sharded</td></tr><tr><td>statistics</td><td>document count, avg document size, storage size, index sizes, capped, WiredTiger cache figures</td></tr></tbody></table><p>Sections that do not apply are omitted, not returned empty.</p><h2 id="_8-explain" tabindex="-1">8. Explain <a class="header-anchor" href="#_8-explain" aria-label="Permalink to &quot;8. Explain&quot;">​</a></h2><p><code>explain(&#39;executionStats&#39;)</code> maps into the existing <code>QueryPlan</code> model: each stage becomes a node, <code>nReturned</code>, <code>docsExamined</code>, <code>keysExamined</code>, <code>executionTimeMillisEstimate</code> and <code>works</code> become its metrics, and rejected plans render as a collapsed sibling list.</p><p>Red-flag rules, written the way ADR-024 requires — what was observed, in numbers, and what it usually means:</p><ul><li><code>COLLSCAN</code> on a collection above a size threshold, reporting how many documents were examined to return how many.</li><li><code>docsExamined ≫ nReturned</code> (default 10×): the index is not selective, or there is none.</li><li>An in-memory <code>SORT</code> stage, with the bytes sorted and the 100 MB limit named.</li><li><code>$lookup</code> executing a nested loop without an index on the foreign field.</li><li>A plan whose winning plan changed between two runs of the same query — the plan-diff view already built in P6-T3 covers it.</li></ul><p><strong><code>explain</code> with <code>executionStats</code> runs the query.</strong> Write operations are therefore explained in <code>queryPlanner</code> mode only, and the destructive guards apply to an explain-analyze exactly as they do to executing it.</p><h2 id="_9-monitoring-and-health" tabindex="-1">9. Monitoring and health <a class="header-anchor" href="#_9-monitoring-and-health" aria-label="Permalink to &quot;9. Monitoring and health&quot;">​</a></h2><p><code>readActivity</code> from <code>$currentOp</code>: opid, namespace, operation, running seconds, client, appName, waiting-for-lock, and the plan summary. <code>stopBackend</code> maps to <code>killOp</code>. Nothing polls by default; the manual/5-second-opt-in rule from ADR-025 applies unchanged.</p><p><code>readHealth</code> findings, read-only with suggested commands, never auto-applied:</p><ul><li>Indexes with zero accesses since the last restart (<code>$indexStats</code>), with the caveat about restart time stated in the finding.</li><li>Redundant indexes — one whose key spec is a prefix of another&#39;s.</li><li>Collections with no index other than <code>_id</code> and a document count above a threshold.</li><li>Unbounded growth signals: storage size ≫ data size (fragmentation), capped collection near its cap, oplog window shorter than a configurable target.</li><li>Connection saturation, WiredTiger read/write ticket exhaustion, cache eviction pressure, from <code>serverStatus</code>.</li><li>TTL index candidates: a Date field present on ~100% of documents in a collection that only ever grows. Stated as a question, not an instruction.</li></ul><h2 id="_10-import-export-notebooks" tabindex="-1">10. Import, export, notebooks <a class="header-anchor" href="#_10-import-export-notebooks" aria-label="Permalink to &quot;10. Import, export, notebooks&quot;">​</a></h2><ul><li><strong>Import</strong>: CSV → collection through <code>bulkLoad</code>, <code>insertMany</code> with <code>ordered: false</code> in batches, with the same coerce-before-writing sample and error report as Postgres (ADR-030). Type mapping is chosen per column and previewed; numbers destined for <code>Decimal128</code> never pass through a JS number. JSON / NDJSON import as well, which is the format people actually have.</li><li><strong>Export</strong>: JSON, Extended JSON (canonical and relaxed), NDJSON, CSV. CSV of nested documents flattens with dotted paths and states the rule in the export dialog, because a silently lossy CSV is worse than a refused one.</li><li><strong>Notebooks</strong>: <code>.pittabook</code> gains a language id per notebook (ADR-029&#39;s one-notebook-one-database rule is unchanged); a MongoDB notebook serializes to a runnable <code>.mongodb.js</code> script.</li></ul><h2 id="_11-safety" tabindex="-1">11. Safety <a class="header-anchor" href="#_11-safety" aria-label="Permalink to &quot;11. Safety&quot;">​</a></h2><p>Everything in <code>05-SECURITY.md</code> applies. The engine-specific additions:</p><ul><li><strong><code>deleteMany</code>/<code>updateMany</code> with <code>{}</code> or a missing filter</strong> is the analogue of <code>DELETE</code> without <code>WHERE</code> — blocked by <code>pitta.query.blockUnqualifiedDml</code> (default on), overridable only by an explicit typed confirmation.</li><li><strong><code>drop</code>, <code>dropDatabase</code>, <code>dropIndex</code>, <code>dropIndexes</code>, <code>renameCollection</code></strong> are destructive.</li><li><strong><code>$out</code> and <code>$merge</code> make an aggregation a write.</strong> An aggregation pipeline reads as harmless and can replace an entire collection. The classifier walks the pipeline for these stages and classifies the command by what it found, not by the method name.</li><li><strong>Server-side JavaScript</strong> — <code>$where</code>, <code>$function</code>, <code>$accumulator</code>, <code>mapReduce</code> — is refused unless <code>pitta.mongodb.allowServerSideJs</code> is turned on, and is always classified as an admin-level operation.</li><li><strong>Values are BSON, never string-concatenated.</strong> Grid filters, cell editors and parameter prompts build BSON values from typed input; there is no code path that renders user input into command text. That is S3.1 for this engine, and the eslint rule that enforces it extends to <code>buildSelect</code>/<code>buildEdits</code> here.</li><li><strong>Read-only profiles</strong> are enforced client-side by refusing any command the classifier calls a write or admin operation, and the connection form recommends a read-only database user, since the server cannot enforce it for us. The tree badge says &quot;client-side only&quot; so nobody mistakes it for a server-side guarantee.</li></ul><h2 id="_12-testing" tabindex="-1">12. Testing <a class="header-anchor" href="#_12-testing" aria-label="Permalink to &quot;12. Testing&quot;">​</a></h2><ul><li><code>docker-compose.test.yml</code> gains MongoDB 6, 7 and 8 services, one of them a single-node <strong>replica set</strong> so transactions and change streams are covered.</li><li><code>test/fixtures/mongo-seed.js</code>: a deliberately nasty corpus — documents with disjoint key sets, every BSON type including Decimal128 at full precision and Int64 at max, deep nesting, arrays of subdocuments, a field that is a string in half the documents and a number in the other half, a field present in 1% of documents, keys needing quoting (dots, <code>$</code>, unicode), a 16 MB document, a 100k-document collection for paging, a 1M-document collection for the benchmark.</li><li>The conformance kit&#39;s <code>EngineFixture</code> for MongoDB; the four non-relational assertions from §13 of <code>10-ENGINE-MODEL.md</code> are exercised here first.</li><li>Property tests: the sampled column union is stable for a given sample; round-tripping every BSON type through the grid&#39;s wire encoding is lossless; missing stays missing.</li></ul><hr><h2 id="_13-what-is-built" tabindex="-1">13. What is built <a class="header-anchor" href="#_13-what-is-built" aria-label="Permalink to &quot;13. What is built&quot;">​</a></h2><p><code>packages/driver-mongodb</code>, registered alongside PostgreSQL, SQLite and Redis.</p><p><strong>Working.</strong> Connecting (standalone, replica set, sharded, SRV; SCRAM, x.509 and AWS IAM; TLS) with the topology probed at connect so transactions and all-or-nothing edits are claimed only where they exist. The tree - databases, collections, views, indexes, and <em>columns sampled from the documents</em>. The document grid, with the sampled column union, one level of dotted flattening, BSON types where Decimal128 and Int64 stay exact, and <code>MISSING</code> rendered apart from <code>NULL</code>. Editing by <code>_id</code> through previewed <code>updateOne</code> / <code>insertOne</code> / <code>deleteOne</code>. The structure view. <code>explain</code> into the shared plan model. <code>$currentOp</code> monitoring with <code>killOp</code>, and a health report from <code>$indexStats</code>, <code>serverStatus</code> and the index shapes. The guards, including the empty-filter check and the <code>$out</code>/<code>$merge</code> pipeline walk.</p><p>And the shell parser: <code>db.orders.find({...}).sort({...}).limit(50)</code> and the constructor literals, parsed with acorn and converted to BSON without ever being executed. The tests that matter most are the refusals - <code>require(&quot;fs&quot;)</code>, <code>process.env</code>, a bare variable, a loop, a computed key - each of which the obvious implementation would have passed by doing the thing being refused.</p><p><strong>Not built yet.</strong> The <code>.mongodb.js</code> editor language, so commands are issued by panels rather than typed into a file, and with it completion, hover and diagnostics from the sampled schema. Tree paging for a database with thousands of collections. <code>readSymbols</code>. The sharding section. The Mongo-specific red-flag rules on top of the plan. The seeded integration fixture and the conformance <code>EngineFixture</code>. Positional array paths when editing. Change streams behind the notifications panel.</p><p>Task-level status is in <code>07-TASKS.md</code> under Phase 12.</p>`,59)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
