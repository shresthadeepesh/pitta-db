import{_ as t,o,c as a,a2 as s}from"./chunks/framework.CZo9GF0W.js";const u=JSON.parse('{"title":"Remote-SSH, WSL and dev containers","description":"","frontmatter":{},"headers":[],"relativePath":"guide/remote.md","filePath":"guide/remote.md","lastUpdated":1789060268000}'),n={name:"guide/remote.md"};function i(r,e,d,h,l,c){return o(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="remote-ssh-wsl-and-dev-containers" tabindex="-1">Remote-SSH, WSL and dev containers <a class="header-anchor" href="#remote-ssh-wsl-and-dev-containers" aria-label="Permalink to &quot;Remote-SSH, WSL and dev containers&quot;">​</a></h1><div class="danger custom-block"><p class="custom-block-title">Read this before debugging a connection</p><p><strong>Pitta connects from the machine VS Code&#39;s workspace is on, not from your laptop.</strong> Over Remote-SSH that is the remote host. In WSL it is the Linux distribution. In a dev container or a Codespace it is the container.</p><p><code>localhost</code> therefore means <em>that</em> machine. This is the single most common reason a connection that works in <code>psql</code> fails in Pitta, and it is not a bug.</p></div><h2 id="why" tabindex="-1">Why <a class="header-anchor" href="#why" aria-label="Permalink to &quot;Why&quot;">​</a></h2><p>Pitta declares <code>&quot;extensionKind&quot;: [&quot;workspace&quot;]</code>, so VS Code installs and runs it on the remote side (ADR-003). That is deliberate and is almost always what you want:</p><ul><li>The database is usually reachable from the remote and not from your laptop — a private subnet, a VPC, a container network.</li><li>Saved queries, notebooks and SQLite files are in the workspace, which is also on the remote.</li><li>Nothing has to be tunnelled twice.</li></ul><p>It has one consequence you have to hold in your head: <strong>every host name in a profile is resolved on the remote.</strong></p><h2 id="install-it-on-the-remote" tabindex="-1">Install it on the remote <a class="header-anchor" href="#install-it-on-the-remote" aria-label="Permalink to &quot;Install it on the remote&quot;">​</a></h2><p>An extension with <code>extensionKind: workspace</code> is not shared from your local install. In the Extensions view, a remote window shows a <strong>Install in SSH: hostname</strong> / <strong>Install in WSL</strong> / <strong>Install in Dev Container</strong> button. Press it. The connections you added locally are in <em>local</em> global state and do not travel; the workspace file <code>.vscode/pitta.connections.json</code> does, because it is part of the workspace.</p><h2 id="the-translation-table" tabindex="-1">The translation table <a class="header-anchor" href="#the-translation-table" aria-label="Permalink to &quot;The translation table&quot;">​</a></h2><table tabindex="0"><thead><tr><th>You want to reach</th><th>Local VS Code</th><th>Remote-SSH window</th><th>WSL window</th><th>Dev container</th></tr></thead><tbody><tr><td>A database on your laptop</td><td><code>localhost</code></td><td>the laptop is not reachable by name — forward a port, or use a VPN</td><td><code>host.docker.internal</code>, or the Windows host IP from <code>/etc/resolv.conf</code></td><td><code>host.docker.internal</code></td></tr><tr><td>A database on the remote host</td><td>forward a port, or use Pitta&#39;s SSH tunnel</td><td><code>localhost</code></td><td>n/a</td><td>n/a</td></tr><tr><td>A database in a sibling Compose service</td><td><code>localhost:&lt;published port&gt;</code></td><td><code>localhost:&lt;published port&gt;</code></td><td><code>localhost:&lt;published port&gt;</code></td><td><strong>the service name</strong>, e.g. <code>postgres:5432</code></td></tr><tr><td>A managed database in the same VPC as the remote</td><td>not reachable</td><td>its private hostname</td><td>n/a</td><td>its private hostname</td></tr></tbody></table><h2 id="remote-ssh" tabindex="-1">Remote-SSH <a class="header-anchor" href="#remote-ssh" aria-label="Permalink to &quot;Remote-SSH&quot;">​</a></h2><p>If VS Code is already on the machine that can reach the database, use a plain connection with the database&#39;s own hostname. You do <strong>not</strong> also need Pitta&#39;s SSH tunnel — that would open a second SSH connection from the remote to somewhere else.</p><p>Pitta&#39;s SSH tunnel is for the case where the database sits behind a <em>further</em> bastion that the workspace machine can reach but not connect through directly. See <a href="/pitta-db/recipes/ssh">the SSH recipe</a>.</p><p><code>~/.ssh/known_hosts</code> is read on the machine Pitta runs on — the remote — so the first tunnel from a fresh remote prompts you to accept a fingerprint even though your laptop has known that key for years.</p><h2 id="wsl" tabindex="-1">WSL <a class="header-anchor" href="#wsl" aria-label="Permalink to &quot;WSL&quot;">​</a></h2><p>A database running in the WSL distribution is <code>localhost</code>. A database running on the Windows host is not: use <code>host.docker.internal</code> if Docker Desktop&#39;s WSL integration is on, or read the host address out of <code>/etc/resolv.conf</code>.</p><p>A Windows-side SQL Server also has to be listening on TCP and permitting the connection through the Windows firewall. Named-pipe and shared-memory protocols do not cross into WSL.</p><h2 id="dev-containers-and-compose" tabindex="-1">Dev containers and Compose <a class="header-anchor" href="#dev-containers-and-compose" aria-label="Permalink to &quot;Dev containers and Compose&quot;">​</a></h2><p>Inside a dev container, a database defined as another service in the same Compose file is reachable <strong>by service name on its internal port</strong> — <code>postgres:5432</code>, not <code>localhost:5433</code>. The published port mapping is for the host, and the container is not the host.</p><p><code>host.docker.internal</code> reaches the machine running Docker, which is how you get back out to something on your laptop. On Linux it needs an explicit <code>extra_hosts: [&quot;host.docker.internal:host-gateway&quot;]</code>.</p><p>Add Pitta to <code>devcontainer.json</code> so a rebuilt container comes back with it:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;customizations&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;vscode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;extensions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;DipeshShrestha.pitta-db&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Full working example in <a href="/pitta-db/recipes/docker">the Docker recipe</a>.</p><h2 id="github-codespaces" tabindex="-1">GitHub Codespaces <a class="header-anchor" href="#github-codespaces" aria-label="Permalink to &quot;GitHub Codespaces&quot;">​</a></h2><p>The same rules as a dev container, plus: the Codespace is in GitHub&#39;s network. A database behind your company VPN is not reachable from it, and a managed database&#39;s firewall will not have the Codespace&#39;s egress address on its allowlist. Either connect through a bastion you have allowed, or use a local window for that connection.</p><h2 id="vscode-dev-and-github-dev" tabindex="-1">vscode.dev and github.dev <a class="header-anchor" href="#vscode-dev-and-github-dev" aria-label="Permalink to &quot;vscode.dev and github.dev&quot;">​</a></h2><p>Pitta does not run there. There is no Node extension host in the browser, and a database client in a browser tab would need a WebSocket proxy you run yourself — a different product (ADR-003). The extension declares limited virtual workspace support so VS Code says so rather than half-loading.</p><h2 id="working-out-where-you-actually-are" tabindex="-1">Working out where you actually are <a class="header-anchor" href="#working-out-where-you-actually-are" aria-label="Permalink to &quot;Working out where you actually are&quot;">​</a></h2><p>VS Code&#39;s own status bar, bottom left, names the window&#39;s remote — <code>SSH: hostname</code>, <code>WSL: Ubuntu</code>, <code>Dev Container: …</code>, or nothing at all when the window is local. That is the machine your connections are made from.</p><p>To confirm what Pitta actually tried, run <strong>Pitta: Show Logs</strong> with <code>pitta.logLevel</code> set to <code>debug</code>: each connection attempt logs its host and port, credentials redacted. If that says <code>localhost</code> and you meant your laptop, this page is the answer.</p>`,30)])])}const m=t(n,[["render",i]]);export{u as __pageData,m as default};
