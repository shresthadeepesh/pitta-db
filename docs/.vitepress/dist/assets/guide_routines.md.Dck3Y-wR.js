import{_ as t,o,c as a,a2 as n}from"./chunks/framework.CZo9GF0W.js";const p=JSON.parse('{"title":"Stored routines","description":"","frontmatter":{},"headers":[],"relativePath":"guide/routines.md","filePath":"guide/routines.md","lastUpdated":1789060268000}'),r={name:"guide/routines.md"};function s(i,e,d,h,c,l){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="stored-routines" tabindex="-1">Stored routines <a class="header-anchor" href="#stored-routines" aria-label="Permalink to &quot;Stored routines&quot;">​</a></h1><p>Procedures and functions are objects you <strong>edit, run and read the output of</strong> — not text you copy out of a DDL view, paste into a scratch buffer and paste back.</p><h2 id="editing" tabindex="-1">Editing <a class="header-anchor" href="#editing" aria-label="Permalink to &quot;Editing&quot;">​</a></h2><p><strong>Edit Routine</strong> on a function or procedure in the explorer opens its definition as a real editor document: syntax, folding, find, a dirty marker, <code>Ctrl+S</code>. What you save is a replacement, and it goes through the same guards as any other write — a routine on a production connection is confirmed exactly as a <code>DELETE</code> there would be, and a read-only connection refuses.</p><p><strong>Compare Routine with Server</strong> puts the definition as it stands on the server beside the one in your editor, which is how you see what a colleague changed under you before you overwrite it.</p><p>What each engine does when you save:</p><table tabindex="0"><thead><tr><th>Engine</th><th>How it replaces</th><th>If the new definition fails to compile</th></tr></thead><tbody><tr><td>PostgreSQL</td><td><code>CREATE OR REPLACE</code></td><td>Nothing happened — DDL is transactional</td></tr><tr><td>SQL Server</td><td><code>DROP</code> then <code>CREATE</code>, in one transaction</td><td>Nothing happened — the drop rolls back</td></tr><tr><td>MySQL / MariaDB</td><td><code>DROP</code> then <code>CREATE</code>, with nothing holding them together</td><td>Pitta puts the old definition back, and says so if it cannot</td></tr></tbody></table><p>MySQL is the one to know about: it has no <code>CREATE OR REPLACE</code> for a routine and cannot roll DDL back, so Pitta asks before it starts and keeps the old text to restore. If the restore fails too, the editor still holds the old definition — do not close it.</p><div class="warning custom-block"><p class="custom-block-title">The overload trap</p><p><code>CREATE OR REPLACE FUNCTION</code> on PostgreSQL only replaces a function with the <strong>same argument types</strong>. Change one and the server happily creates a <em>second</em> function beside the first, reports success, and every existing call keeps reaching the old one. Pitta checks the routine after saving, says so when this has happened, and offers to drop the old overload.</p></div><p>A failure on save is put on the line of the <strong>body</strong> the server complained about, not at the top of the file. PostgreSQL and SQL Server both report one; MySQL reports none, and Pitta shows the message without inventing a line.</p><h2 id="running" tabindex="-1">Running <a class="header-anchor" href="#running" aria-label="Permalink to &quot;Running&quot;">​</a></h2><p><strong>Run Routine</strong> — from the explorer, or from the routine&#39;s own editor tab — asks for the arguments first, in the same form the <a href="./query-mode#parameters">parameter prompt</a> uses. It knows the arguments from the catalog, so each one arrives with its declared name and type, <code>OUT</code> parameters are not asked for, and an argument with a default is marked optional.</p><p>The call itself is built by the driver, because the same idea is three different statements:</p><table tabindex="0"><thead><tr><th>Engine</th><th>Procedure</th><th>Function</th></tr></thead><tbody><tr><td>PostgreSQL</td><td><code>CALL s.p($1, NULL)</code></td><td><code>SELECT s.f($1)</code>, or <code>SELECT * FROM s.f($1)</code> when it returns a set</td></tr><tr><td>SQL Server</td><td><code>EXEC s.p @a = @1, @b = @out OUTPUT</code> plus the select that reads <code>@out</code> back</td><td><code>SELECT s.f(@1)</code>, or <code>SELECT * FROM s.f(@1)</code> for a table-valued one</td></tr><tr><td>MySQL</td><td><code>CALL s.p(?, @out)</code> then <code>SELECT @out</code></td><td><code>SELECT s.f(?)</code></td></tr></tbody></table><p>Values are <strong>bound as parameters</strong>, never pasted into the statement. Results, cancellation, history and the row grid are the same as any other run.</p><h2 id="watching-one-run" tabindex="-1">Watching one run <a class="header-anchor" href="#watching-one-run" aria-label="Permalink to &quot;Watching one run&quot;">​</a></h2><p>A procedure that takes a minute is the reason this exists. While it runs, every message the server sends arrives in the results panel <strong>as it is sent</strong>:</p><ul><li>PostgreSQL — <code>RAISE NOTICE</code>, <code>RAISE WARNING</code>, and the rest</li><li>SQL Server — <code>PRINT</code>, and <code>RAISERROR … WITH NOWAIT</code></li><li>MySQL / MariaDB — warnings, including <code>SIGNAL SQLSTATE &#39;01000&#39; SET MESSAGE_TEXT = &#39;…&#39;</code></li></ul><p>That last one is worth knowing about: MySQL has no notice channel at all, so a <code>SIGNAL</code> is the only way a procedure can report progress, and it is only visible because Pitta asks for the warnings immediately — the next statement clears them.</p><p>When a statement fails inside a routine, the engine&#39;s own stack is shown under <strong>Where</strong>: which routine, and which line of its body, innermost first. That is usually enough to open the routine and go straight to the line.</p><div class="tip custom-block"><p class="custom-block-title">Not a step debugger</p><p>There are no breakpoints here yet. What there is: the definition in a real editor, the call built for you, every message as it happens, and failures pointed at a line.</p></div>',21)])])}const g=t(r,[["render",s]]);export{p as __pageData,g as default};
