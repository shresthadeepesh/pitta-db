import{_ as i,o as e,c as a,a2 as t}from"./chunks/framework.CZo9GF0W.js";const c=JSON.parse('{"title":"10 — Multi-Model Engine Contract (driver contract v2)","description":"","frontmatter":{},"headers":[],"relativePath":"10-ENGINE-MODEL.md","filePath":"10-ENGINE-MODEL.md","lastUpdated":1789060268000}'),n={name:"10-ENGINE-MODEL.md"};function h(d,s,l,r,o,p){return e(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="_10-—-multi-model-engine-contract-driver-contract-v2" tabindex="-1">10 — Multi-Model Engine Contract (driver contract v2) <a class="header-anchor" href="#_10-—-multi-model-engine-contract-driver-contract-v2" aria-label="Permalink to &quot;10 — Multi-Model Engine Contract (driver contract v2)&quot;">​</a></h1><p><code>03-DRIVER-API.md</code> describes the contract as it shipped for PostgreSQL and SQLite. Both are relational, both speak SQL, and the contract quietly assumes it in five places. This document is the delta that removes those assumptions so MongoDB (Phase 12) and Redis (Phase 13) can be drivers rather than forks.</p><p>It is written as a <em>contract change</em>, deliberately: ADR-021 established that core changes forced by a new engine must be a decision with a label on the PR, not a quiet edit. Everything here is that decision, made once, up front, with the engines that motivate it named.</p><hr><h2 id="_1-where-sql-is-currently-assumed" tabindex="-1">1. Where SQL is currently assumed <a class="header-anchor" href="#_1-where-sql-is-currently-assumed" aria-label="Permalink to &quot;1. Where SQL is currently assumed&quot;">​</a></h2><table tabindex="0"><thead><tr><th>#</th><th>Assumption</th><th>Where</th><th>Breaks on</th></tr></thead><tbody><tr><td>1</td><td>A statement is SQL text with positional params</td><td><code>Session.query(sql, params)</code>, <code>Statement { sql, params }</code></td><td>Mongo (BSON command documents), Redis (argv arrays)</td></tr><tr><td>2</td><td>Introspection is a query whose rows map to nodes</td><td><code>NodeDefinition.query()</code> + <code>map()</code>; core executes the SQL</td><td>Redis (<code>SCAN</code> is a cursor loop), Mongo (<code>listCollections</code> is a command)</td></tr><tr><td>3</td><td>A result is a fixed set of columns × rows</td><td><code>FieldMeta[]</code> + <code>Row = unknown[]</code>, wire cell <code>string | null</code></td><td>Mongo (heterogeneous documents, missing ≠ null), Redis (a value is not a row)</td></tr><tr><td>4</td><td>The tree vocabulary is a closed enum of relational kinds</td><td><code>OBJECT_KINDS</code></td><td><code>collection</code>, <code>key</code>, <code>keyspace</code>, <code>stream</code>, <code>consumerGroup</code></td></tr><tr><td>5</td><td>Safety classification comes from a SQL grammar</td><td><code>classifyStatement</code>, <code>splitStatements</code></td><td><code>FLUSHALL</code>, <code>db.c.deleteMany({})</code>, <code>$out</code></td></tr></tbody></table><p>Each is addressed below. Nothing here removes a guarantee: the exact-decimal rule (ADR-004), the params-not-interpolation rule (S3.1), the staged-edit preview, and the production guards all survive, and in two cases get stronger.</p><hr><h2 id="_2-engines-declare-a-language" tabindex="-1">2. Engines declare a language <a class="header-anchor" href="#_2-engines-declare-a-language" aria-label="Permalink to &quot;2. Engines declare a language&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EngineLanguage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** VS Code language id contributed by the driver&#39;s extension bundle. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                              // &#39;sql&#39; | &#39;mongodb&#39; | &#39;redis&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  displayName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                     // &#39;SQL&#39; | &#39;MongoDB shell&#39; | &#39;Redis commands&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  fileExtensions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [&#39;.sql&#39;] | [&#39;.mongodb.js&#39;] | [&#39;.redis&#39;]</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  lineComment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                     // &#39;--&#39; | &#39;//&#39; | &#39;#&#39;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Used only when the driver supplies no splitter of its own. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  fallbackSplit</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;semicolon&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;newline&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;none&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>DriverAdapter</code> gains <code>readonly language: EngineLanguage</code>, and <code>Statement.sql</code> is renamed <code>Statement.text</code>. The text is in <em>the engine&#39;s</em> language; core never inspects it, and every place that used to say &quot;SQL&quot; in a variable name or a UI string reads the language&#39;s <code>displayName</code> instead.</p><p>Query mode, CodeLens, the connection binding, history, saved queries and notebooks are already text-and-connection shaped. They become language-generic: a saved query is a file with the engine&#39;s extension, a notebook cell carries the language id, and the status bar shows <code>connection · database · language</code>.</p><p>Statement splitting and formatting move behind optional driver methods:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">splitStatements</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { text: string; start: number; end: number }[]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">formatCommand</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span></code></pre></div><p><strong>As built.</strong> Every shipped driver supplies both, the SQL ones included: the scanner and the printer stay in core as shared code taking a <code>SqlDialect</code>, and each SQL driver passes its own (ADR-060). <code>POSTGRES_DIALECT</code> is what core falls back to for a buffer that declares SQL and has no connection to ask, which is the whole of what <code>fallbackSplit: &#39;semicolon&#39;</code> now means.</p><h2 id="_3-introspection-may-be-imperative-and-is-paged" tabindex="-1">3. Introspection may be imperative, and is paged <a class="header-anchor" href="#_3-introspection-may-be-imperative-and-is-paged" aria-label="Permalink to &quot;3. Introspection may be imperative, and is paged&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NodeDefinition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Existing declarative form; still preferred where the catalog is queryable.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NodeContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NodeQuery</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  map</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">row</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Row</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NodeContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TreeNodeData</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** For engines whose catalog is not a query. Mutually exclusive with query/map. */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  fetch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">session</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NodeContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">page</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TreePage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">nodes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TreeNodeData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">nextCursor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  children</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectKind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  minServerVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  folderLabel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TreePage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">cursor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">limit</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>Paging is not a Redis nicety. A Redis database holds millions of keys and there is no <code>LIMIT</code>; the only correct traversal is a <code>SCAN</code> cursor. The tree therefore gains a <strong>Load more</strong> node and a <code>pitta.explorer.treePageSize</code> setting, and the same mechanism caps a MongoDB database with 20 000 collections.</p><p>Core keeps ownership of caching, version gating, and invalidation either way, because <code>fetch</code> returns the same <code>TreeNodeData</code> the declarative path does.</p><h2 id="_4-the-tree-vocabulary-opens-up" tabindex="-1">4. The tree vocabulary opens up <a class="header-anchor" href="#_4-the-tree-vocabulary-opens-up" aria-label="Permalink to &quot;4. The tree vocabulary opens up&quot;">​</a></h2><p><code>ObjectKind</code> becomes <code>string</code>. The relational kinds stay as exported constants (<code>OBJECT_KINDS</code> remains, as the <em>known</em> set), and a driver declares anything extra:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NodeKindDescriptor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  pluralLabel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Codicon id. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  icon</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  canOpenData</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  canOpenStructure</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">readonly nodeKinds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readonly NodeKindDescriptor[]   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// on DriverAdapter</span></span></code></pre></div><p>The alternative — extending the enum for every engine — makes every new engine a core edit forever, which is precisely the failure ADR-001 exists to prevent. See ADR-036.</p><p>New kinds the first two non-relational drivers introduce: <code>collection</code>, <code>keyspace</code>, <code>keyPrefix</code>, <code>key</code>, <code>stream</code>, <code>consumerGroup</code>, <code>serverNode</code>.</p><p><strong>As built.</strong> <code>ObjectKind</code> is <code>(typeof OBJECT_KINDS)[number] | (string &amp; {})</code>, which is <code>string</code> with the known kinds still autocompleting. The tree asks <code>describeNodeKind(driver, kind)</code> for a label, a plural, an icon and <code>canOpenData</code>, layered per field: the driver&#39;s descriptor, then core&#39;s vocabulary, then the kind string Title Cased with a neutral icon - so a level nobody has described yet is readable rather than blank. <code>canOpenStructure</code> was not needed: the structure tab is drawn from whatever <code>readStructure</code> returned, so an engine that returns nothing for a kind already has no tab. The conformance kit asserts that every kind a plan can emit is either a core kind or one the driver described, which is the typo check ADR-036 promised in place of the compiler&#39;s.</p><h2 id="_5-results-declare-a-shape" tabindex="-1">5. Results declare a shape <a class="header-anchor" href="#_5-results-declare-a-shape" aria-label="Permalink to &quot;5. Results declare a shape&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ResultShape</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rows&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;documents&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;keyValue&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;message&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;none&#39;</span></span></code></pre></div><p><code>rows</code> is today&#39;s behavior. <code>documents</code> means the fields were derived by sampling rather than declared by the server. <code>keyValue</code> is a single value with no column structure (a Redis <code>GET</code>). <code>message</code> is a status reply (<code>OK</code>, <code>PONG</code>, <code>{ acknowledged: true }</code>) — rendered as a message, not as a one-cell grid.</p><p><code>FieldMeta</code> gains two optional members:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** Dotted path into a document, e.g. [&#39;address&#39;,&#39;city&#39;]. */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readonly string[]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/** True when this column came from sampling, not from a declared schema. */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">inferred</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> boolean</span></span></code></pre></div><p>For a <code>documents</code> result the driver projects a <strong>column union from a sample</strong> (default 200 documents, <code>pitta.documents.sampleSize</code>), ordered <code>_id</code> first then by frequency. The raw document is always available in the detail pane and is always the last view offered, which is ADR-033&#39;s rule applied one level up: a projection that quietly differs from what is stored would be worse than no projection, and the raw view is what settles the question. See ADR-038.</p><h2 id="_6-missing-is-not-null" tabindex="-1">6. Missing is not null <a class="header-anchor" href="#_6-missing-is-not-null" aria-label="Permalink to &quot;6. Missing is not null&quot;">​</a></h2><p>Postgres has no way for a row to lack a column. A document does, and <code>{ a: null }</code> and <code>{}</code> are different documents that different queries match. Collapsing them would be the same class of bug as rendering <code>NULL</code> as an empty string, which ADR-004 already forbids.</p><p>The wire cell type becomes:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> WireCell</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">missing</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>The grid renders a <code>MISSING</code> token, styled distinctly from <code>NULL</code>. The filter row offers <code>exists</code> / <code>does not exist</code> separately from <code>IS NULL</code>, so <code>FILTER_OPS</code> gains <code>exists</code>, <code>notExists</code>, and <code>regex</code>. A cell editor can clear a field to null (<code>$set: null</code>) or remove it (<code>$unset</code>), and those are two different menu items with two different generated commands. Engines that cannot distinguish the two declare <code>distinguishesMissing: false</code> and never see the token. See ADR-039.</p><p><strong>As built.</strong> Removing lives in the cell detail pane rather than in a cell editor: clearing a cell is typing, and removing a field is an action about the record, so it sits beside Filter and Copy where the field is named. It reaches the driver as core&#39;s <code>MISSING</code> inside <code>RowEdit.set</code> - the same value a read puts in a cell the record does not have - and the host refuses it on an engine that declared <code>distinguishesMissing: false</code> rather than writing NULL and calling it the same thing.</p><h2 id="_7-paging-gains-a-cursor-mode" tabindex="-1">7. Paging gains a cursor mode <a class="header-anchor" href="#_7-paging-gains-a-cursor-mode" aria-label="Permalink to &quot;7. Paging gains a cursor mode&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">page</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;keyset&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; after</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readonly unknown[] }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;offset&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">offset</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: number }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;cursor&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; cursor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> string }</span></span></code></pre></div><p><code>QueryHandle</code> gains <code>nextCursor(): string | undefined</code>. Redis <code>SCAN</code>/<code>HSCAN</code> and a MongoDB cursor both page this way, and neither can report a total or jump to an offset — so the grid&#39;s page control renders as <em>Next / Previous within this session</em> rather than as numbered pages when <code>supportsCursorPaging &amp;&amp; !supportsOffsetPaging</code>.</p><p><strong>As built.</strong> The switch is <code>!supportsOffsetPaging</code> alone: an engine with offsets keeps the control that can jump even where it also has a cursor, because Redis pays for its offsets in a <code>SKIP</code> the user can see rather than in a page control that cannot move (ADR-049). In cursor mode the grid drops the jump to the first page, numbers rows within the page rather than against a total it does not have, and hides the exact count — which the host refuses outright rather than answering with a full scan. Previous is served by remembering the cursors already handed out; nothing rewinds one. The host also stops asking for one row more than the page holds, since that extra row cannot be given back.</p><p>The Redis case also has no server-side sort and no server-side filter over a hash scan. Two flags say so, and the grid hides the affordances rather than sorting one page and implying the whole key was sorted.</p><h2 id="_8-capability-additions" tabindex="-1">8. Capability additions <a class="header-anchor" href="#_8-capability-additions" aria-label="Permalink to &quot;8. Capability additions&quot;">​</a></h2><p>Added to <code>DriverCapabilities</code>, all defaulting to <code>false</code> in <code>MINIMAL_CAPABILITIES</code> so an engine claims nothing by accident:</p><table tabindex="0"><thead><tr><th>Flag</th><th>Meaning</th></tr></thead><tbody><tr><td><code>supportsTransactions</code></td><td>A transaction with rollback exists at all</td></tr><tr><td><code>supportsAtomicEdits</code></td><td>A staged-edit batch applies all-or-nothing</td></tr><tr><td><code>schemaless</code></td><td>Records in one container may have different fields</td></tr><tr><td><code>distinguishesMissing</code></td><td>Absent and null are different values</td></tr><tr><td><code>supportsCursorPaging</code> / <code>supportsOffsetPaging</code></td><td>Which page modes are real</td></tr><tr><td><code>supportsServerSideSort</code> / <code>supportsServerSideFilter</code></td><td>Whether the grid may offer them</td></tr><tr><td><code>supportsAggregation</code></td><td>A pipeline/aggregation surface worth its own affordances</td></tr><tr><td><code>supportsTtl</code></td><td>Records expire; TTL is shown and editable</td></tr><tr><td><code>supportsSecondaryIndexes</code></td><td>An index tree branch and an advisor are meaningful</td></tr><tr><td><code>resultShapes</code></td><td>Which of the shapes above this engine ever returns</td></tr></tbody></table><p><code>parameterStyle</code> gains <code>&#39;none&#39;</code> for engines that take values rather than placeholders. <code>isolationLevels</code> may be empty.</p><h2 id="_9-capabilities-are-static-then-refined-by-the-session" tabindex="-1">9. Capabilities are static, then refined by the session <a class="header-anchor" href="#_9-capabilities-are-static-then-refined-by-the-session" aria-label="Permalink to &quot;9. Capabilities are static, then refined by the session&quot;">​</a></h2><p>Some of these are not knowable until connect. MongoDB has multi-document transactions only on a replica set or a sharded cluster. Redis in cluster mode cannot run a multi-key <code>MULTI</code> across slots, and a connection under an ACL user may be unable to run <code>CONFIG</code> or <code>CLIENT LIST</code> at all — the same server, two users, two different sets of true statements.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Overrides for facts only knowable after connecting. */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> capabilities</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Partial</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DriverCapabilities</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Core resolves <code>driver.capabilities</code> overlaid with <code>session.capabilities</code> and the UI reads the resolved value. This also retires a small Postgres wart: version gating for feature flags currently lives in call sites. See ADR-035.</p><p><strong>As built.</strong> <code>resolveCapabilities</code> is core&#39;s; the host reaches it through <code>ConnectionManager.capabilitiesFor(profileId, database?)</code>, which overlays an <em>already-open</em> session and never connects - a question about what a connection can do must not be the thing that dials a production database. Every UI gate reads it: the server-activity command, the notification panel, and the data view&#39;s row-id fallback, document shape, atomicity promise and page mode. Before a session exists the answer is the engine&#39;s flags, which is the honest answer to &quot;what could this connection do&quot;.</p><h2 id="_10-guards-become-engine-classified" tabindex="-1">10. Guards become engine-classified <a class="header-anchor" href="#_10-guards-become-engine-classified" aria-label="Permalink to &quot;10. Guards become engine-classified&quot;">​</a></h2><p><code>classifyStatement</code> keeps its shape and its rule from ADR-027 — a command is destructive if <em>either</em> the driver&#39;s classifier or the shared heuristic says so, and neither can downgrade the other — but the driver-side half becomes explicit:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CommandClassification</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;read&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;write&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;ddl&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;admin&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;transaction&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;unknown&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  destructive</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** The analogue of DML with no WHERE: affects every record in the container. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  unfiltered</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  targets</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectRef</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** Shown verbatim in the confirmation dialog. */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">classifyCommand</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">session</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CommandClassification</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span></span></code></pre></div><p>Two rules that are new, and that matter more here than they did for SQL:</p><ol><li><strong>Unknown fails closed on production.</strong> A SQL statement that does not parse keeps the heuristic&#39;s answer, because the heuristic is good on SQL. There is no comparable heuristic for an arbitrary Redis module command or an unrecognised <code>db.runCommand</code>, so on a <code>prod</code>-tagged connection an <code>unknown</code> classification is treated as destructive and requires the typed confirmation. On dev it is allowed with a single confirm.</li><li><strong>Classification comes from the engine where the engine offers it.</strong> Redis reports <code>write</code>, <code>admin</code>, <code>dangerous</code>, <code>blocking</code> and <code>noscript</code> flags through <code>COMMAND INFO</code>, read once at connect. That is the same principle as ADR-026&#39;s &quot;use the server&#39;s own grammar&quot;, applied to a different engine&#39;s own metadata. See ADR-040.</li></ol><h2 id="_11-editing-without-transactions" tabindex="-1">11. Editing without transactions <a class="header-anchor" href="#_11-editing-without-transactions" aria-label="Permalink to &quot;11. Editing without transactions&quot;">​</a></h2><p><code>buildDml</code> becomes <code>buildEdits</code>, returning the same <code>Statement[]</code> (now <code>{ text, params }</code>) in the engine&#39;s language. The staged-edit model, the dirty highlighting, and the preview-before-apply flow are unchanged — the preview shows <code>db.users.updateOne({_id: ObjectId(&quot;…&quot;)}, {$set: {…}})</code> or <code>HSET session:42 status active</code> instead of an <code>UPDATE</code>.</p><p>What changes is the promise. With <code>supportsAtomicEdits: false</code> the apply dialog says, in those words, that the changes are applied in order and cannot be rolled back, and that a failure stops the batch leaving earlier changes in place. It does not silently degrade a guarantee the user was taught to expect from the Postgres panel. See ADR-041.</p><p>For engines with no affected-row count, S4.5&#39;s verification degrades to a read-back of the edited records where a key exists, and to nothing where it does not — stated in the dialog rather than assumed.</p><h2 id="_12-what-is-not-generalized" tabindex="-1">12. What is <em>not</em> generalized <a class="header-anchor" href="#_12-what-is-not-generalized" aria-label="Permalink to &quot;12. What is *not* generalized&quot;">​</a></h2><ul><li><strong>No cross-engine query.</strong> Joining a Postgres table to a Mongo collection is a different product. Reaffirmed, not revisited.</li><li><strong>ERD and schema diff stay relational.</strong> They are gated on <code>supportsSchemaDiff</code> and foreign keys. A Mongo &quot;relationship&quot; inferred from field naming is a guess, and a diagram of guesses is worse than no diagram. Index and validator diffing for MongoDB is a separate, honest feature — [P2].</li><li><strong>The SQL parser stays in <code>driver-pg</code>.</strong> Nothing about generalization moves grammar into core.</li><li><strong><code>generateDdl</code> stays optional.</strong> For MongoDB it returns a runnable <code>createCollection</code> + <code>createIndexes</code> script; Redis has no schema to generate and declares the flag false.</li></ul><h2 id="_13-conformance" tabindex="-1">13. Conformance <a class="header-anchor" href="#_13-conformance" aria-label="Permalink to &quot;13. Conformance&quot;">​</a></h2><p><code>packages/core/test/driver-conformance.ts</code> currently issues SQL. It becomes engine-agnostic: each driver supplies an <code>EngineFixture</code> that can create a container, write records of the engine&#39;s types, and name them back.</p><p>The kit then asserts the same behaviors it always did — connect/close, cancel mid-operation, stream a large result without buffering, every declared type round-trips, introspection returns a well-formed tree, capability flags match observed behavior — plus four that only exist now:</p><ul><li>a <code>documents</code> result with heterogeneous keys projects to a stable column union</li><li>a missing field survives round-trip as missing, not as null</li><li>an <code>unknown</code> command is refused on a <code>prod</code> profile</li><li>with <code>supportsAtomicEdits: false</code>, a failing batch reports exactly which edits were applied</li></ul><p><strong>A driver is still &quot;supported&quot; only when it passes the kit against real servers in CI.</strong> No aspirational drivers.</p>`,67)])])}const g=i(n,[["render",h]]);export{c as __pageData,g as default};
